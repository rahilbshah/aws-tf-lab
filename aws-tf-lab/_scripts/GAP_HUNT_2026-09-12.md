# Gap hunt — 2026-09-12

Generated by a 30-agent workflow: diagnose -> draft -> adversarially refute.
**Nothing here has been applied.** Notes were read-only to every agent.

Diagnosis: {'PRESENT_BUT_BURIED': 9, 'MISSING': 18, 'BANK_CONFLICT': 1, 'NOTE_WRONG': 1, 'PRESENT_AND_CLEAR': 1}

Verdicts: {'NARROWED': 32, 'UPHELD': 10, 'REFUTED': 5}

## Recommended to apply

Selection rule: anything actively wrong, plus every PRESENT_BUT_BURIED (the fact is
already in the note - this only moves it somewhere the self-test generator can drill it,
so it costs almost no length), plus MISSING items backed by 2+ questions.


## RECOMMENDED  (7)

### [MISSING · high · 2 qid(s)] 01-iam.md — An IAM Group is never the landing point for a federated identity — IAM groups contain only IAM users, so in an "our users already exist in AD" question the identity half of the answer is always an IAM Role

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/singlesignon/latest/userguide/permissionsetsconcept.html

**Proposed:**
```markdown
> [!warning] Trap — "IAM Groups" in a federation question
> The word *group* carries two meanings in these questions and only one of them is an answer. An **IAM group is a container of IAM users and nothing else** — AWS: *"User groups can't be nested; they can contain only users, not other IAM groups"*, and *"You cannot identify a user group as a `Principal` in a policy … because groups relate to permissions, not authentication."* So a federated identity — an AD user arriving via IAM Identity Center or SAML — can **never** land in an IAM group; there is no IAM user for it to be a member of.
> In "our users already exist in Active Directory", the group being mapped is the **AD group**, and the thing it maps to is always an **IAM role** (via IAM Identity Center: *"When you assign a permission set, IAM Identity Center creates corresponding IAM Identity Center-controlled IAM roles in each account"*). If an option offers "IAM Groups" as the identity landing point, it is the distractor; if an option offers "configure an IAM role and a policy", that is the half of the answer you must select, not skip.
```

<details><summary>adversary reasoning</summary>

I tried to break this one and could not. I fetched https://docs.aws.amazon.com/IAM/latest/UserGuide/id_groups.html myself and all three quoted strings are verbatim and in context: "An IAM user group is a collection of IAM users", "User groups can't be nested; they can contain only users, not other IAM groups", and "You cannot identify a user group as a Principal in a policy (such as a resource-based policy) because groups relate to permissions, not authentication, and principals are authenticated IAM entities."

The refutation I looked for was a path by which a federated identity does reach an IAM group. There is none: SAML and OIDC federation both terminate in `sts:AssumeRoleWithSAML` / `AssumeRoleWithWebIdentity` on a role, a role cannot be a group member (groups contain only users), and the Identity Center path ends in a role too — I verified independently at the permission sets page: "When you assign a permission set, IAM Identity Center creates corresponding IAM Identity Center-controlled IAM roles in each account, and attaches the policies specified in the permission set to those roles." So "the thing the AD group maps to is always an IAM role" is exactly right.

The one ambiguity worth watching is the word "group" doing double duty — Identity Center's identity store has groups, and you do assign permission sets to them. The draft handles this correctly by scoping its claim to *IAM* groups and naming the AD group as the thing being mapped, so it does not mislead.

Internal consistency: reinforces, does not contradict. 01-iam.md already says "AD groups map to IAM roles" in prose and in the "create IAM users for the on-premises staff" trap; this callout supplies the *mechanism* for why the group option is impossible, and it targets a different distractor than the ex

</details>

---

### [MISSING · high · 3 qid(s)] 07-rds-aurora.md — Aurora Global Database vs cross-Region Aurora read replica for DR

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Replication.html

**Proposed:**
```markdown
### Cross-Region DR — Aurora Global Database vs cross-Region read replica

|   | Aurora Global Database | Cross-Region Aurora read replica |
|---|---|---|
| Engines | Aurora MySQL **and** Aurora PostgreSQL | **Aurora MySQL only** — AWS: "Aurora PostgreSQL doesn't support cross-Region Aurora Replicas" |
| What does the replicating | the **storage layer**, on dedicated infrastructure — the engine is not involved | **binlog replication** replayed by the database engine (you must turn `binlog_format` on first) |
| Replication lag | **typically under 1 second** | higher — engine-driven, over a longer network path |
| RPO | **seconds** on failover; **0** on switchover (secondaries are synced first) | no published figure — whatever the binlog lag happens to be when the Region dies |
| RTO | **minutes** — the secondary "assumes the primary role within a few minutes" | you promote it yourself: instances reboot, **several minutes or longer**, *after* a human notices and decides |
| Promotion | **managed** — `failover-global-cluster` (unplanned) / `switchover-global-cluster` (planned) | **manual** — `promote-read-replica-db-cluster`; replication stops and the cluster can never be a replication target again |
| Endpoint after promotion | the **global writer endpoint does not change** | the promoted cluster becomes standalone — repoint the app at **its own cluster endpoint** |
| How many | **10** secondary Regions (each can hold **16** readers, not 15) | **5** cross-Region read replica clusters per source cluster |
| Trigger words | "cross-Region DR", "lowest RTO/RPO", "minimal operational overhead" | "read scaling in another Region", "migrate from one Region to another" |
```

<details><summary>adversary reasoning</summary>

I fetched all four underlying pages myself. Most cells verify verbatim: 'For each source DB cluster, you can have up to five cross-Region DB clusters that are read replicas'; 'up to 10 secondary read-only DB clusters ... Each secondary DB cluster can include up to 16 (rather than 15) Aurora Replicas'; 'Replication ... is handled by the Aurora storage layer rather than by the database engine, so lag time ... is minimal—typically, less than 1 second'; 'RTO can be in the order of minutes'; 'RPO is typically measured in seconds'; 'Because this feature synchronizes secondary DB clusters with the primary before making any other changes, RPO is 0 (no data loss)'; 'Typically, the chosen secondary cluster assumes the primary role within a few minutes'; 'This endpoint has a value that remains the same even when a new Region becomes the primary'; 'you must turn on binary logging ... update the binlog_format parameter'; 'The promotion process can take several minutes or longer'; 'the promoted DB cluster is no longer a read replica, you can't use it as a replication target'. TWO DEFECTS. (1) ENGINE SCOPE — the whole right-hand column is Aurora MySQL only. The source page is titled 'Replicating Amazon Aurora MySQL DB clusters across AWS Regions', and Aurora.Replication.html states flatly: 'Aurora PostgreSQL doesn't support cross-Region Aurora Replicas.' The draft table never says this, so a learner memorising it would believe Aurora PostgreSQL offers cross-Region read replicas — exactly the kind of false fact this pass exists to stop. (2) MISATTRIBUTED ENDPOINT DETAIL — the cell 'new cluster endpoint — drop the -ro and repoint the app' is lifted from the Global Database DR page, where the -ro renaming describes promoting a global database SECONDARY cluster. The cross-Region read repl

</details>

---

### [NOTE_WRONG · high · 1 qid(s)] 02-ec2.md — "Maximum possible I/O performance" → EC2 instance store, not provisioned-IOPS EBS. Replace the Performance row in the EBS vs Instance Store table.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/ec2/latest/instancetypes/so.html

**Replaces (verbatim in note):**
```markdown
| Performance | Up to ~256K IOPS (`io2 Block Express`) | Very high local NVMe; usually faster than non-provisioned EBS |
```

**Proposed:**
```markdown
| Performance | **Bounded by the network** — `io2 Block Express` tops out at 256,000 IOPS / 4,000 MiB/s **per volume** (Nitro instances only), and the instance's own EBS connection caps it again | **Higher raw IOPS, lower latency** — disks physically attached to the host, no network hop. Same `i7i.48xlarge`: 240,000 IOPS over EBS (16 KiB I/O) vs 7,200,000 random-read IOPS on local NVMe (4 KiB I/O) |
```

<details><summary>adversary reasoning</summary>

Substance verified independently. I curl'd the storage-optimized instance types page myself and grepped the raw tables rather than trusting the drafter's quote. Instance store table: 'i7i.48xlarge | 12 x 3750 GB | NVMe SSD | 7,200,000 / 3,960,000'. EBS table: 'i7i.48xlarge | 60000.00 | 7500.00 | 240000.00'. Also verified the instance-level cap statement: 'An instance's EBS performance is bounded by the instance's performance limits, or the aggregated performance of its attached volumes, whichever is smaller.' io2 Block Express 256,000 IOPS / 4,000 MiB/s confirmed on the EBS volume-types page, with footnote 3 'Nitro-based instances support volumes provisioned with up to 256,000 IOPS.'

THREE OVERSTATEMENTS narrowed:
(1) `contradicts_note: true` is wrong. The existing row does NOT 'point the other way' — it says instance store is 'Very high local NVMe; usually faster than non-provisioned EBS'. That understates and hedges; it does not assert io2 wins. The fix is still worth making (the hedge is misleading), but this is an improvement, not a correction of a false statement — which matters because the human should not be told their note was wrong when it was merely weak.
(2) The two IOPS figures are NOT measured at the same block size, and the draft presents them as directly comparable. Verified column headers I read myself: instance store table is 'the aggregated IOPS performance with 4,096 byte block size at queue depth saturation', EBS table is 'Baseline / Maximum IOPS (16 KiB I/O)'. The gap is still ~30x and survives the adjustment, but a memorised note must not hide the mismatch.
(3) 'Highest IOPS and lowest latency on EC2' as a bolded superlative is the drafter's synthesis — InstanceStorage.html says only 'physically attached to the host computer' and 'ideal for tempor

</details>

---

### [PRESENT_BUT_BURIED · high · 1 qid(s)] 02-ec2.md — The instance is bound to the role by an instance profile — an EC2 instance is not a valid trust-policy principal; the trust policy names the service principal ec2.amazonaws.com.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/iam-roles-for-amazon-ec2.html

**Proposed:**
```markdown
> [!warning] Trap — add the EC2 instance to the role's trust policy so it can assume the role
> No — an individual EC2 instance is not one of the principal types a `Principal` element accepts
> (account/root, IAM role, role session, IAM user, federated principal, AWS service, `*`), so there
> is nothing to add. For EC2 the trust policy names the *service*:
> `"Principal": { "Service": "ec2.amazonaws.com" }` — the same one line for every instance in the
> account. What binds **this** instance to **that** role is the **instance profile**: a container
> holding exactly one role, passed as `iam_instance_profile` at launch or attached to a running
> instance. One role per profile, one role per instance, but the same role can sit in many profiles.
> To change an instance's permissions, *replace* its instance profile — removing a role from a
> profile takes up to an hour to take effect. See [[01-iam]].
```

<details><summary>adversary reasoning</summary>

I tried hard to refute this and could not. The one promising counter-example is real and the drafter missed it: AWS does mint a per-instance principal ARN — 'Each Amazon EC2 instance that you launch has an instance identity role', with ARN 'arn:aws:iam::{account}:assumed-role/aws:ec2-instance/{instance-id}'. That looks like an instance-as-principal. It is not usable as one: the same page says instance identity roles 'have no role-trust policy document, and are not subject to any identity or resource policy.' So the draft's claim survives.

Principal taxonomy verified on the IAM reference page: 'You can specify any of the following principals in a policy: AWS account and root user; IAM roles; Role sessions; IAM users; Federated user principals; AWS services; All principals' — no instance. Service principal format confirmed: '{{service-name}}.amazonaws.com'. Instance-profile mechanics confirmed on the EC2 page: 'Amazon EC2 uses an instance profile as a container for an IAM role'; 'An instance profile can contain only one IAM role. You can include an IAM role in multiple instance profiles'; 'You can only attach one IAM role to an instance, but you can attach the same role to many instances.'

NARROWED on lean-ness, per §13.11 and §13.7. The note already states the instance-profile fact FOUR times (prose line 102, TL;DR line 141, console map line 160, MCQ 2) — but never inside a table or a trap, so it is never drilled. That justifies the callout; it does not justify a fifth full restatement plus a seven-item taxonomy recital. Cut the enumeration of principal types to one clause (the taxonomy is IAM-note material, not EC2-note material) and led with the drillable half. One substantive addition: the docs warn 'We do not recommend removing a role from an instance profile, beca

</details>

---

### [PRESENT_BUT_BURIED · medium · 1 qid(s)] 05-vpc-core.md — A VPC has exactly two DNS attributes — enableDnsSupport and enableDnsHostnames — both required for a Route 53 private hosted zone and for private DNS on interface endpoints

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/vpc/latest/userguide/AmazonDNS-concepts.html

**Proposed:**
```markdown
> [!warning] Trap — inventing a third VPC DNS attribute
> A VPC has **exactly two** DNS attributes; any other attribute name is made up. **`enableDnsSupport`** (console: *DNS resolution*) — can instances resolve through the Amazon-provided DNS server at VPC CIDR **+2** (or `169.254.169.253`); default **`true`**. **`enableDnsHostnames`** (console: *DNS hostnames*) — do instances with a public IPv4 address also get a public DNS hostname; default **`false`** unless it's the default VPC. **Both must be `true`** for a Route 53 **private hosted zone** and for **private DNS on interface endpoints** — note it's private DNS that needs them, not the endpoint itself: with private DNS off the endpoint still works, you just have to call its `*.vpce.amazonaws.com` name instead of the normal service hostname. Terraform: `enable_dns_support` / `enable_dns_hostnames` on `aws_vpc`.
```

<details><summary>adversary reasoning</summary>

Every factual claim holds. I fetched AmazonDNS-concepts.html myself: 'The following VPC attributes determine the DNS support provided for your VPC' introduces a two-row table — enableDnsHostnames ('Determines whether the VPC supports assigning public DNS hostnames to instances with public IP addresses. The default for this attribute is false unless the VPC is a default VPC') and enableDnsSupport ('Determines whether the VPC supports DNS resolution through the Amazon provided DNS server ... The default for this attribute is true'), plus 'If you use custom DNS domain names defined in a private hosted zone in Amazon Route 53, or use private DNS with interface VPC endpoints (AWS PrivateLink), you must set both the enableDnsHostnames and enableDnsSupport attributes to true.' The same page gives the resolver addresses (VPC CIDR +2 and 169.254.169.253). I confirmed there is no third VPC DNS attribute via vpc-dns-updating.html, which exposes only these two in the console. What I am narrowing is the packaging, not the fact. The draft asserts that 'enableDnsDomain', 'enableVpcSupport' and 'enableVpcHostnames' 'are all distractors' — those three names are the drafter's own inventions, not anything sourced from AWS or from any exam, and printing three fabricated API names in bold in a note the human MEMORISES is precisely the way a fake name gets remembered as real. I kept the exactly-two framing (which is doc-supported) and cut the invented names. I also added two things I verified first-hand and that make the trap more useful than the version proposed: the console labels differ from the API names ('DNS resolution' / 'DNS hostnames' per vpc-dns-updating.html — the kind of console-vs-API mismatch CLAUDE.md §3 asks to flag), and the fallback when private DNS is off, since privatelin

</details>

---

### [PRESENT_BUT_BURIED · high · 1 qid(s)] 10-route53.md — An alias record can target another record of the same type in the same hosted zone — that is how the apex points at www.example.com.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/Route53/latest/APIReference/API_ResourceRecordSet.html

**Replaces (verbatim in note):**
```markdown
> [!warning] Trap — CNAME at the apex
> `example.com` must carry SOA and NS records, and DNS forbids a CNAME coexisting with any other record at the same name. So a CNAME at the apex is **invalid DNS**, not an AWS limitation. Pointing `example.com` at an ALB or CloudFront is exactly what **alias** records exist for.
```

**Proposed:**
```markdown
> [!warning] Trap — CNAME at the apex
> `example.com` must carry SOA and NS records, and DNS forbids a CNAME coexisting with any other record at the same name. So a CNAME at the apex is **invalid DNS**, not an AWS limitation. Pointing `example.com` at an ALB or CloudFront is exactly what **alias** records exist for — and so is pointing it at **`www.example.com`**: **another record in the same hosted zone is a legal alias target**, so apex → www is an **alias**, never a CNAME, even though `www` is a plain hostname and not an AWS resource. Two consequences: an alias must be the **same type as its target**, so at the apex the target may not itself be a `CNAME` record (below the apex a CNAME-type alias is fine — every type except `NS` and `SOA` is allowed); and an alias to a record in the same zone uses **that target record's TTL**.
```

<details><summary>adversary reasoning</summary>

The core claim is correct and I verified it independently in two places: the developer guide ('They also let you route traffic from one record in a hosted zone to another record'; 'Another Route 53 record of the same type in the same hosted zone'), and the TTL rule ('If an alias record points to another record in the same hosted zone, Route 53 uses the TTL of the target record'; the API reference adds 'If you're creating or updating an alias resource record set, omit TTL. Amazon Route 53 uses the value of TTL for the alias target'). One clause in the draft over-generalises and had to be narrowed: 'the alias must be the same record type as its target, so the target itself can't be a CNAME' reads as a universal rule, but the CNAME prohibition is specific to the apex. The Route 53 API reference is explicit that for 'Another resource record set in this hosted zone' you 'Specify the type of the resource record set that you're creating the alias for. All values are supported except NS and SOA' — i.e. a CNAME-type alias to a CNAME record is legal below the apex; only at the apex is it blocked, 'because the alias record must have the same type as the record you're routing traffic to, and creating a CNAME record for the zone apex isn't supported even for an alias record.' My final_text keeps the same-type rule but scopes the CNAME consequence to the apex, which is the trap's subject anyway. Consistent with the note's existing 'TTL | can't set one — uses the target's' row and line 195's target list.

</details>

---

### [PRESENT_BUT_BURIED · medium · 1 qid(s)] 01-iam-advanced.md — SCPs do not affect service-linked roles — the third exemption alongside the management account, even though SCPs DO cap a member account's root user. Correct in the note but buried in a compound bullet, so the self-test generator never drills it.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_scps.md

**Proposed:**
```markdown
> [!warning] Trap — SCPs and service-linked roles
> **SCPs do not affect service-linked roles** — in any account, member accounts included. AWS is flat about it: *"SCPs do not affect any service-linked role. Service-linked roles enable other AWS services to integrate with AWS Organizations and can't be restricted by SCPs."* This is the exemption people miss, because they memorise the root-user asymmetry and stop there. Line the three up: the **management account** is exempt; **service-linked roles** are exempt; a **member account's root user is not** — *"SCPs affect all users and roles in attached accounts, including the root user."* (The full exempt list also covers registering for Enterprise Support as root, CloudFront trusted-signer functionality, and reverse DNS for Lightsail/EC2 as root — see Key facts.)
```

<details><summary>adversary reasoning</summary>

The fact and the diagnosis both check out. I fetched the SCP page myself and it says verbatim: 'SCPs do not affect any service-linked role. Service-linked roles enable other AWS services to integrate with AWS Organizations and can't be restricted by SCPs.' and 'SCPs affect all users and roles in attached accounts, including the root user. The only exceptions are those described in Tasks and entities not restricted by SCPs.' and 'An SCP restricts permissions for IAM users and roles in member accounts, including the member account's root user.' I also confirmed the placement diagnosis by reading _scripts/_selftest.py directly: _tables() takes only markdown tables with >=4 pipes in the header and >=2 body rows, and _traps() matches only `> [!warning] ...` callouts. Key-facts bullets are invisible to it, so the note's line-196 bullet has indeed never been drilled. Nothing here is a correction to the note — line 196 is accurate.

WHERE I NARROWED IT — the draft's closing sentence, 'So the exempt set is **the management account + service-linked roles**', is under-inclusive and would contradict the note's own line 196. The docs' 'Tasks and entities not restricted by SCPs' list has more entries: any action by the management account, anything via a service-linked role, registering for Enterprise Support as root, CloudFront trusted-signer functionality, reverse DNS for a Lightsail email server / EC2 instance as root, plus tasks on some AWS-related services (Alexa Top Sites, Alexa Web Information Service, Mechanical Turk, Product Marketing API). An internally inconsistent note is worse than an incomplete one, and _selftest.py lifts trap bodies verbatim into answers, so that sentence would have been drilled as a two-item exempt set against a five-item bullet twenty lines earlier. R

</details>

---

## HOLD — single-question evidence, would bloat a lean note  (35)

### [? · ? · 0 qid(s)] 01-iam.md — IAM database authentication is the answer to "short-lived credentials to the database / no stored password / use the EC2 instance profile credentials" — and it already encrypts the connection, so an SSL option is never the answer

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/UsingWithRDS.IAMDBAuth.html

**Proposed:**
```markdown
### How an application authenticates to RDS

|   | Password in config | Password in Secrets Manager | **IAM database authentication** |
|---|---|---|---|
| What the app presents to the DB | a long-lived password | a password fetched at runtime from the secret | an **authentication token, lifetime 15 minutes** |
| Credential stored anywhere? | yes, forever | yes — but Secrets Manager rotates it on a schedule (a **Lambda rotation function** for application credentials; **managed rotation**, no Lambda, for RDS/Aurora *master user* credentials) | **no password at all** |
| Who the DB is really trusting | whoever holds the password | whoever may read the secret | the caller's **IAM role** — e.g. the EC2 instance profile or the Lambda role |
| IAM action required | — | `secretsmanager:GetSecretValue` | **`rds-db:connect`** on `arn:aws:rds-db:…:dbuser:…` |
| Traffic encrypted in transit? | only if you configure SSL/TLS yourself | only if you configure SSL/TLS yourself | **yes** — AWS lists it as a benefit: *"Network traffic to and from the database is encrypted using Secure Socket Layer (SSL) or Transport Layer Security (TLS)"*. Not the same as "no TLS setup": the client still points at the RDS CA bundle (`--ssl-ca` / `sslrootcert`) |
| Extra setup beyond IAM | — | rotation function with network access to the DB | **enable IAM DB auth on the instance/cluster**, **create the DB user** — MySQL/MariaDB `CREATE USER 'jane_doe' IDENTIFIED WITH AWSAuthenticationPlugin AS 'RDS';`, PostgreSQL `CREATE USER db_userx; GRANT rds_iam TO db_userx;` — and pass the CA bundle from the client |
| Engines | all | all | **RDS MariaDB, MySQL, PostgreSQL; Aurora MySQL, Aurora PostgreSQL** — not Oracle, not SQL Server |
| Exam trigger | — | "rotate the database password automatically" | "**short-lived / temporary credentials** to the database", "**no password stored** in the application", "use the **EC2 instance's profile credentials** to reach the database" |
```

<details><summary>adversary reasoning</summary>

I fetched four AWS pages myself rather than re-reading the drafter's. Confirmed verbatim: RDS guide — "IAM database authentication works with MariaDB, MySQL, and PostgreSQL", "Each token has a lifetime of 15 minutes", "Network traffic to and from the database is encrypted using Secure Socket Layer (SSL) or Transport Layer Security (TLS)", "For applications running on Amazon EC2, you can use profile credentials specific to your EC2 instance to access your database instead of a password"; Aurora guide — "IAM database authentication works with Aurora MySQL, and Aurora PostgreSQL"; DBAccounts page — both CREATE USER forms exactly as drafted. So the core of the table survives. Two things do not.

(1) The concept headline's "it already encrypts the connection, so an SSL option is never the answer" overreaches. AWS's own documented connect command for IAM auth is `mysql --host=... --ssl-ca=<full path to SSL certificate> --enable-cleartext-plugin --user=jane_doe --password=$TOKEN` (https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/UsingWithRDS.IAMDBAuth.Connecting.AWSCLI.html), and the DBAccounts page adds "To allow SSL connections, modify the user account with ... ALTER USER 'jane_doe'@'%' REQUIRE SSL;". TLS config does not vanish; the client still points at the RDS CA bundle. A note that teaches "IAM DB auth means no SSL setup" would be memorised wrong.

(2) The Secrets Manager column asserts rotation happens "via a Lambda rotation function" without qualification. AWS: "Some services offer managed rotation, where the service configures and manages rotation for you. With managed rotation, you don't use an AWS Lambda function" — and Amazon RDS and Amazon Aurora are both on that list for master user credentials (https://docs.aws.amazon.com/secretsmanager/latest/userguide/ro

</details>

---

### [? · ? · 0 qid(s)] 01-iam.md — SSL/TLS options are encryption, not authentication — and IAM DB auth already encrypts; a role alone is only half the answer; the token is an RDS token, not STS

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/UsingWithRDS.IAMDBAuth.Connecting.AWSCLI.html

**Proposed:**
```markdown
> [!warning] Trap — SSL/TLS is not authentication
> "The application must connect **without a stored database password**" is answered by **IAM database authentication**, never by an SSL/TLS option. `--ssl-ca`, "configure SSL", "force TLS" change how the connection is *encrypted*; they say nothing about *who may log in*. IAM DB auth brings the encryption with it — AWS: *"Network traffic to and from the database is encrypted using Secure Socket Layer (SSL) or Transport Layer Security (TLS)"* — but that is **not** the same as "no TLS setup": AWS's own connect command still passes the RDS CA bundle (`mysql … --ssl-ca=global-bundle.pem --enable-cleartext-plugin`). The point is which *question* the option answers, not that TLS configuration disappears.
> Two sibling distractors on the same stem:
> - **"Create an IAM role and attach it to the EC2 instances"** — only half the answer. You must also **enable IAM DB authentication on the DB instance** *and* **create the matching DB user** (`IDENTIFIED WITH AWSAuthenticationPlugin AS 'RDS'` for MySQL/MariaDB, `GRANT rds_iam` for PostgreSQL). A role alone logs in to nothing.
> - **"Use STS"** — what the database sees is not an STS token. It is an RDS auth token (`aws rds generate-db-auth-token`), SigV4-signed, **valid 15 minutes**, passed as the password. STS is still upstream — the instance-profile credentials that sign the token are STS credentials, and they must still be valid at connect time — but STS is not what you present to the database.
```

<details><summary>adversary reasoning</summary>

The trap's spine is right and two of its three claims verify cleanly, but the sentence "They are also redundant here" is wrong and has to go.

Refuted part: I fetched the AWS connect page myself. Its general format for connecting with an IAM token is `mysql --host={hostName} --port={portNumber} --ssl-ca={full_path_to_ssl_certificate} --enable-cleartext-plugin --user={userName} --password={authToken}`, with `--ssl-ca` documented as "The full path to the SSL certificate file that contains the public key" and a link to download it. The DBAccounts page adds "Database network traffic is encrypted using SSL/TLS when using IAM. To allow SSL connections, modify the user account with the following command. ALTER USER 'jane_doe'@'%' REQUIRE SSL;". So `--ssl-ca` is not redundant under IAM DB auth — it is a required parameter of AWS's own documented command. Telling the human it is redundant would plant a false fact that also makes them doubt the correct exam heuristic when they meet the real CLI.

Upheld parts: "a role alone logs in to nothing" is supported — the troubleshooting note says an `AWSAuthenticationPlugin is not loaded` error means "verify that you are using a supported configuration and that you have enabled IAM database authentication on your DB instance", and for PostgreSQL "You must grant the rds_iam role to use IAM authentication." The STS point is also right — "Authentication tokens are generated using AWS Signature Version 4" and the token comes from `aws rds generate-db-auth-token`, not an STS API — but I narrowed it, because STS is genuinely upstream here: the same page warns "If you are using temporary credentials when creating an IAM database authentication token, the temporary credentials must still be valid when using the IAM database authentication token t

</details>

---

### [MISSING · medium · 2 qid(s)] 01-iam-advanced.md — The multi-account consolidation sequence — which requirement Organizations/consolidated billing, account invitations, IAM Identity Center + AD Connector, cross-account roles, and SCPs each answer

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/singlesignon/latest/userguide/account-instances-identity-center.html

**Proposed:**
```markdown
### Which multi-account requirement does this solve?

| The requirement in the stem | The answer | The detail that decides it |
|---|---|---|
| "one bill across all our accounts", volume discounts, shared RIs / Savings Plans | **AWS Organizations — consolidated billing** | the **management account becomes responsible for all charges** accrued by member accounts; the member's own payment method stops being used. Combined usage *"shares the volume pricing discounts, Reserved Instance discounts, and Savings Plans"* |
| "bring our **existing** accounts under one organization" | **Invite** each account from the management account; its owner accepts | invitations can be sent **only from the management account**, expire after **15 days**, and an account can join **only one organization**. Accounts *created* by Organizations join automatically |
| "one sign-on for all accounts, using our on-prem AD" | **IAM Identity Center**, with **AD Connector** (or AWS Managed Microsoft AD) as the identity source | an **organization instance must be enabled in the Organizations management account**; **permission sets** — which become IAM roles in each account — need one, since *"Account instances do not support permission sets and therefore do not support access to AWS accounts"*. The directory must reside in the management account (or the delegated admin account, if one exists) |
| "divisions keep their own accounts, corporate IT keeps oversight" | **cross-account IAM role** in each member account trusting the management account | the built-in one is **`OrganizationAccountAccessRole`** — created automatically for accounts Organizations **creates**, and **not** created for **invited** accounts, where you add it yourself |
| "stop anyone in these accounts from doing X" | **SCP** | caps only — it answers **none of the rows above**. No billing, no sign-on, no access granted |
```

<details><summary>adversary reasoning</summary>

Every load-bearing claim verified against pages I fetched myself, and one of them I found a stronger source for than the drafter used.

Organizations invitations page (fetched): "You can send an invitation to other accounts only from the management account of your organization", "Each invitation must be responded to within 15 days, or it expires", "An account can only join one organization. If you receive multiple invitations, you can accept only one", "After an account accepts the invitation to join an organization, the management account of the organization becomes responsible for all charges accrued by the new member account. The payment method attached to the member account is no longer used", and "For invited member accounts, AWS Organizations doesn't automatically create the IAM role OrganizationAccountAccessRole". The cross-account role page confirms the other half: "When you create a member account using the AWS Organizations console, AWS Organizations automatically creates an IAM role named OrganizationAccountAccessRole in the account."

Identity Center: "Your organization instance must be enabled in your management account" (organization-instances page, fetched). The drafter's "permission sets are available only for organization instances" is true, but I found a blunter first-party statement than the one cited — the account instances page: "Account instances do not support permission sets and therefore do not support access to AWS accounts." The AD prerequisite verifies including the parenthetical: "You must have an existing AD Connector or AWS Managed Microsoft AD directory set up in AWS Directory Service, and it must reside within your AWS Organizations management account", and the same page's delegated-admin bullet: "the directory must reside in (be owned b

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — What stops or delays ASG replacement — Standby, suspendable process types (ReplaceUnhealthy vs HealthCheck vs ScheduledActions), grace period, and impaired status

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/autoscaling/ec2/userguide/understand-how-suspending-processes-affects-other-processes.html

**Proposed:**
```markdown
### What stops the ASG replacing an instance

|   | What it does to replacement | When you use it or hit it |
|---|---|---|
| **`Standby` lifecycle state** | Amazon EC2 Auto Scaling performs **no health checks at all** on the instance, and the load balancer deregisters it. It stays in the group and you keep paying for it. | Patch, reboot or troubleshoot **one** instance without it being terminated. Decrement desired capacity when you enter standby, or the ASG launches a replacement alongside it. |
| **Suspend the `ReplaceUnhealthy` process** | Instances **are still marked unhealthy** — they just aren't replaced. Resume it and everything marked unhealthy in the meantime is replaced at once. | Group-wide pause while you investigate. |
| **Suspend the `HealthCheck` process** | Stops *marking* instances unhealthy from EC2 and ELB health checks. **Custom health checks keep working.** | Pair it with `ReplaceUnhealthy` — that pair is what AWS recommends when you don't want instances replaced at all. |
| **Grace period not expired** | `health_check_grace_period` is the minimum time a new instance is kept in service before being terminated for failing a health check. **Exception: an instance no longer in the EC2 `running` state is marked unhealthy and replaced immediately, grace period or not.** | Boot + bootstrap time. It also applies to instances coming back from standby and instances you attach by hand. |
| **Status check `impaired`** | The ASG **waits a few minutes for AWS to fix the issue** instead of marking the instance unhealthy straight away. A status check of `insufficient-data` never marks it unhealthy. | "Why hasn't it replaced my obviously broken instance yet?" — usually this. |
| **`health_check_type = "EC2"` (default)** | The ELB verdict is ignored entirely, so an app-level failure never triggers replacement. | The zombie-instance default — see the health-check split above. |

*`ScheduledActions` is in the same list of suspendable processes but has nothing to do with health: suspending it only pauses scheduled scaling.*
```

<details><summary>adversary reasoning</summary>

I fetched all four source pages myself rather than trusting the drafter's quotes, and every row survives. (1) Standby — as-enter-exit-standby.html: 'Amazon EC2 Auto Scaling does not perform health checks on instances that are in a standby state'; 'If there is a load balancer target group or Classic Load Balancer attached to your Auto Scaling group, the instance is deregistered from the load balancer'; 'You are billed for instances that are in a standby state'; 'If you choose not to decrement the desired capacity of the Auto Scaling group, Amazon EC2 Auto Scaling launches an instance to replace the one on standby.' (2) ReplaceUnhealthy suspended — verbatim: 'Amazon EC2 Auto Scaling stops replacing instances that are marked as unhealthy. Instances that fail EC2 or Elastic Load Balancing health checks are still marked as unhealthy. As soon as you resume the ReplaceUnhealthy process, Amazon EC2 Auto Scaling replaces instances that were marked unhealthy while this process was suspended.' (3) HealthCheck suspended — verbatim: 'Amazon EC2 Auto Scaling stops marking instances unhealthy as a result of EC2 and Elastic Load Balancing health checks. Your custom health checks continue to function properly.' (4) The 'suspend ReplaceUnhealthy and HealthCheck' recommendation is verbatim in health-checks-overview.html: 'If you do not want instances to be replaced, we recommend that you suspend the ReplaceUnhealthy and HealthCheck process for individual Auto Scaling groups.' (5) impaired/insufficient-data — verbatim in the same page's Important callout. (6) Grace period definition and the running-state exception — verbatim in health-check-grace-period.html, including the three classes of instance it applies to. (7) 'By default, Amazon EC2 Auto Scaling ignores the results of the Elastic L

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — "The ASG isn't replacing my unhealthy instance" / "patch one instance without it being replaced" — Standby and suspend ReplaceUnhealthy are the answers; suspend ScheduledActions is the distractor

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/autoscaling/ec2/userguide/as-enter-exit-standby.html

**Proposed:**
```markdown
> [!warning] Trap — "the ASG isn't replacing my unhealthy instance"
> Four things suppress or delay replacement, and the stem tells you which: the instance is in **`Standby`** (Auto Scaling runs **no** health checks on it), the **`ReplaceUnhealthy`** process is suspended (still marked unhealthy, just not replaced), the **health check grace period** hasn't expired, or the status check is **`impaired`** and Auto Scaling is waiting a few minutes for AWS to fix it. Run the same question backwards — "patch one instance without the ASG killing it" — and **`Standby` is the cleaner answer**: AWS documents it as letting you stop, start or reboot an instance "without worrying about Amazon EC2 Auto Scaling terminating the instances as part of its health checks **or during scale-in events**." Suspending **`ReplaceUnhealthy`** also stops health-check replacement, but it is group-wide and leaves the instance eligible for **scale-in**, which runs through the separate `Terminate` process. Suspending **`ScheduledActions`** is the distractor: it only pauses scheduled scaling and has nothing to do with health. The one thing no suspension delays is an instance that has left the `running` state — that is replaced immediately, grace period or not.
```

<details><summary>adversary reasoning</summary>

The four suppressors are all confirmed against pages I fetched myself, and so is the ScheduledActions distractor. What the draft overstates is the reversed half: it calls Standby and suspending `ReplaceUnhealthy` 'two right answers' to 'patch one instance without the ASG killing it'. They are not equivalent, and AWS says so. The standby page states Standby protects against two different kinds of termination — 'This feature helps you stop and start the instances or reboot them without worrying about Amazon EC2 Auto Scaling terminating the instances as part of its health checks **or during scale-in events**.' Suspending `ReplaceUnhealthy` covers only the first: the suspended-processes page scopes it to 'stops replacing instances that are marked as unhealthy', and scale-in runs through the separate `Terminate` process, which is still live. So a suspended `ReplaceUnhealthy` leaves the instance you are patching eligible to be picked for scale-in, and it is group-wide rather than per-instance. On a two-answer multi-select both may key; on a single-answer stem Standby is the one that satisfies 'without the ASG killing it' unconditionally. Presenting them as interchangeable is exactly the kind of near-neighbour blur this vault's self-test exists to prevent, so I have narrowed the sentence rather than dropped the callout. Everything else is unchanged and verified: the running-state exception is verbatim from health-check-grace-period.html ('During the health check grace period, if Amazon EC2 Auto Scaling detects that an instance is no longer in the Amazon EC2 running state, it immediately marks the instance Unhealthy and replaces it'), and the impaired wait is verbatim from health-checks-overview.html. No conflict with the existing trap run — it sits cleanly after the 'failed AL

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — Vertical vs horizontal scaling — only horizontal fixes availability; AWS Compute Optimizer is the named tool for the vertical half

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/design-principles.html

**Proposed:**
```markdown
### Vertical vs horizontal scaling

|   | Vertical — scale **up** | Horizontal — scale **out** |
|---|---|---|
| What changes | one instance gets a bigger instance type | more instances of the same size |
| How you do it | **stop the instance**, change the type, start it — there is downtime | an ASG adds instances across 2+ AZs behind an ALB |
| Named AWS tool | **AWS Compute Optimizer** — rightsizing recommendations (see [[13-cost-optimization]]) | ASG + ALB — this note |
| Fixes availability? | **No.** Still one box, still a single point of failure | **Yes.** "Replace one large resource with multiple small resources" |
| Ceiling | the largest instance type that exists | the ASG's `max_size` |
```

<details><summary>adversary reasoning</summary>

Every cell verified against pages I fetched. 'You must stop your instance before you can change its instance type. Ensure that you plan for downtime while your instance is stopped' — change-instance-type-of-ebs-backed-instance.html, verbatim. 'If you want a recommendation for an instance type that is best able to handle your existing workload, you can use AWS Compute Optimizer' — ec2-instance-resize.html, verbatim (I fetched this page separately because the drafter's Compute Optimizer quote is not on the change-instance-type page; it checks out on the resize page). 'AWS Compute Optimizer is a service that analyzes your AWS resources' configuration and utilization metrics to provide you with rightsizing recommendations and identify idle resources' — what-is-compute-optimizer.html, verbatim. 'Scale horizontally to increase aggregate workload availability: Replace one large resource with multiple small resources to reduce the impact of a single failure on the overall workload' — Well-Architected reliability pillar design principles, verbatim. I grepped the whole vault: 'vertical' appears in no note, so this axis is genuinely absent, and the §13.3 deduplication call is right — 13-cost-optimization.md already carries the 14-day Compute Optimizer fact (lines 130, 177) with its own verified doc link, and the draft correctly wiki-links instead of restating it. Two small caveats for the human, neither large enough to narrow: (a) the 'stop, change type, start' row is specifically the **EBS-backed** path; an instance-store-backed instance cannot be resized in place at all (ec2-instance-resize.html routes it to 'Migrate to a new instance type'), and a Spot Instance's type cannot be changed — but 02-ec2.md already covers the stop/start mechanics at line 217 and this row is not contr

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — Baking an AMI into a launch template is plumbing for the horizontal answer, not an independent remedy for a CPU-saturated single server

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/change-instance-type-of-ebs-backed-instance.html

**Proposed:**
```markdown
> [!warning] Trap — baking an AMI is not the second fix
> A stem that says "the server is pegged at 100% CPU **and** is a single point of failure" is asking for both axes: horizontal (**ASG + ALB across AZs**) for availability, and vertical (**right-size the instance type — AWS Compute Optimizer** is the named tool) for the CPU. Creating an AMI and referencing it in a launch template is **how you build the horizontal answer**, not an independent remedy — pick it as well and you have chosen the same fix twice and left the undersized instance type unaddressed. (Baking an AMI *is* the right fix for a different problem — slow boot, and the grace-period boot loop above — just not for a CPU ceiling.) Note which way the dependency runs: vertical scaling needs the instance **stopped**, and Amazon EC2 Auto Scaling marks a stopped member `Unhealthy` and may terminate and replace it — so resize inside an ASG only after putting the instance in **`Standby`** or suspending the scaling processes.
```

<details><summary>adversary reasoning</summary>

Two problems, both fixable, neither fatal. First, an overstatement of fact: the draft says a stopped instance inside an ASG 'will be marked unhealthy and replaced'. The EC2 doc I fetched says 'the Amazon EC2 Auto Scaling service marks the stopped instance as unhealthy, and **might** terminate it and launch a replacement instance' — marking is certain, replacement is not (and `ReplaceUnhealthy`/`Terminate` may be suspended, or maintenance-policy throttling may defer it). I softened 'will be replaced' to 'may terminate and replace it'. Second, and more important for a note the human memorises: the draft's remedy clause names only 'suspend the scaling processes', which contradicts the companion trap directly above it, where Standby is the headline answer for exactly this operation — and the standby page is the one that explicitly covers stop/start. I added Standby as the first option. Third, a bounding clause: this note already argues hard that baking an AMI is 'the clearest argument for golden images there is' (grace-period failure mode, line 281), and a flat 'baking an AMI is not the fix' risks the human over-generalising to the boot-loop case where it *is* the fix. The narrowed text says which problem an AMI does solve. The residual caution the human should weigh: the trap's core claim — that in a stem offering both, the AMI option is subsumed by the ASG option — is exam-strategy reasoning derived from a question bank, not an AWS-documented fact, and per the vault's own rule the bank shapes what you ask, never what is true. The factual spine (Compute Optimizer is the rightsizing tool; resize requires a stop; a stopped ASG member is marked unhealthy) is all first-party verified; the 'same fix twice' inference is judgement and should read as judgement.

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — BANK CONFLICT 2025-8-T5-Q63 — adjudication only, no note edit proposed

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/autoscaling/ec2/userguide/ec2-auto-scaling-scheduled-scaling.html

**Proposed:**
```markdown

```

<details><summary>adversary reasoning</summary>

The vault verdict is correct and I am upholding it: no note edit. I fetched the scheduled-scaling page myself and it confirms both of the note's claims verbatim — 'A scheduled action sets the group's desired, minimum, and maximum capacity at the date and time specified. You can create a scheduled action for only one of these capacities at a time, for example, desired capacity' and 'You can use scheduled scaling and scaling policies together... After a scheduled scaling action runs, the scaling policy can continue to make decisions about whether to further scale capacity. While your application scales to match demand, current capacity must fall within the minimum and maximum capacity that was set by your scheduled action.' The same fetch also re-verifies the note's 125-action limit, 5-field cron field order, UTC default with IANA time zone and DST adjustment, unique-start-time rule and the up-to-two-minutes delay — all of line 232 stands. The concept is already carried in a trap callout, the Scaling policy types table, a worked example and the TL;DR, so adding it again would duplicate. What I am narrowing is the drafter's framing of the bank side, which is stale and would waste the human's time if relayed as-is. This is NOT a new discovery: the key was already found through the confident-wrong route and corrected on **5 September 2026**. `aws-saa-trainer/bank/questions.json` now carries `answer_idx: [0]` / `answer_letters: ["A"]` for 2025-8-T5-Q63, `tools/key-correction-2025-8-T5-Q63.json` records `status: verified -> corrected` with the same AWS-docs reasoning, and `mistakes.md` line 285 logs it ('Seventeen questions were queued as suspect keys... Sixteen held. One did not, and the human was right'). So there is no surviving letter-vs-text contradiction in the record, n

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — An ALB listener port is unique — "force HTTPS" on an ALB already serving :80 means MODIFYING the existing :80 listener's default action, not creating a new one

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/elasticloadbalancing/latest/APIReference/API_CreateListener.html

**Replaces (verbatim in note):**
```markdown
> [!warning] Trap — redirect on the wrong load balancer, or the wrong direction
> `redirect` is an **ALB** (Layer 7) listener action; an **NLB** operates at Layer 4 and cannot inspect or rewrite HTTP, so "redirect HTTP to HTTPS on an NLB" is always wrong. And the redirect only runs one way: HTTP→HTTPS is supported, **HTTPS→HTTP is not**.
```

**Proposed:**
```markdown
> [!warning] Trap — redirect on the wrong load balancer, the wrong direction, or a second listener on an occupied port
> `redirect` is an **ALB** (Layer 7) listener action; an **NLB** operates at Layer 4 and cannot inspect or rewrite HTTP, so "redirect HTTP to HTTPS on an NLB" is always wrong. The redirect only runs one way: HTTP→HTTPS is supported, **HTTPS→HTTP is not**. And **a port carries exactly one listener** — `CreateListener` on a port that already carries a *different* listener fails with **`DuplicateListener`: "A listener with the specified port already exists."** So when the stem says the ALB *already* accepts HTTP on :80, "add a new :80 listener with a redirect action" is not an available move; you **modify the existing :80 listener's `default_action`** from `forward` to `redirect` (`ModifyListener` — AWS: "To update a listener, use ModifyListener"; in Terraform, edit the `default_action` block of the existing `aws_lb_listener`). Two listeners is the greenfield build, one edit is the brownfield one — read the stem for which it is.
```

<details><summary>adversary reasoning</summary>

I fetched API_CreateListener myself and confirmed verbatim: Errors → "DuplicateListener — A listener with the specified port already exists. HTTP Status Code: 400", and "To update a listener, use ModifyListener." I also fetched the ALB guide's load-balancer-listeners.html independently; it confirms "Every listener has a default action, also known as the default rule. The default rule can't be deleted" but says nothing about port uniqueness, so the API reference is the load-bearing source. The core claim survives: a port carries exactly one listener, and the brownfield move is ModifyListener, not CreateListener.

What the draft overstates: it says CreateListener on an occupied port *fails* with DuplicateListener, full stop. The same page states — and the drafter even quoted this without acting on it — "This operation is idempotent, which means that it completes at most one time. If you attempt to create multiple listeners with the same settings, each call succeeds." So an *identical* call succeeds (returning the existing listener); it is a call with *different* settings (e.g. redirect where forward exists) that returns DuplicateListener. That is exactly the case the trap describes, so the teaching point stands, but the flat "fails" is wrong as written and this note is memorised. Narrowed to "a port with a *different* listener".

Internal consistency: the note already teaches the two-listener (:80 redirect + :443 forward) build at lines 46-53 and in the console↔Terraform map (line 190). The addition could read as a contradiction, but the draft's closing "greenfield vs brownfield" sentence reconciles them explicitly, so I kept it. Exam relevance is high and specific: the note's own weak-spots list records "ALB `redirect` listener action — missed while marked _sure_ ... Dis

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — What cross-zone load balancing OFF does to distribution: DNS splits client traffic evenly across per-AZ nodes, and each node splits its share only among targets in its own AZ

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/elasticloadbalancing/latest/userguide/how-elastic-load-balancing-works.html

**Proposed:**
```markdown
### Cross-zone load balancing — on vs off

AWS's own worked example: two enabled AZs, **2 targets in AZ-A, 8 in AZ-B**. Route 53 hands clients the node addresses, so with round robin **each node receives 50% of the client traffic either way**. What cross-zone changes is where a node is allowed to send its share.

|   | Cross-zone ON | Cross-zone OFF |
|---|---|---|
| A node sends to | registered targets in **all** enabled AZs | registered targets in **its own AZ only** |
| The arithmetic | 100% ÷ all targets | 50% ÷ targets in *that* AZ |
| Per-target share (2 in AZ-A, 8 in AZ-B) | **10% each — all 10 equal** | **AZ-A 25% each, AZ-B 6.25% each** |
| Which LB is here by default | **ALB** — always on at the LB level (can only be turned off per target group) | **NLB and GWLB** — `load_balancing.cross_zone.enabled` defaults to `false` |

**Unequal target counts per AZ + cross-zone off = unequal per-target load.** "Even across targets" is the ON arithmetic; "even across AZs" is the OFF arithmetic, and the per-target number then depends on how many neighbours share that AZ's node. (For what it costs, see `Trap — cross-zone billing`.)
```

<details><summary>adversary reasoning</summary>

I fetched how-elastic-load-balancing-works.html myself and the arithmetic is verbatim correct, every number: "There are two enabled Availability Zones, with two targets in Availability Zone A and eight targets in Availability Zone B... each load balancer node receives 50% of the traffic from the clients"; "If cross-zone load balancing is enabled, each of the 10 targets receives 10% of the traffic"; "If cross-zone load balancing is disabled: Each of the two targets in Availability Zone A receives 25% of the traffic. Each of the eight targets in Availability Zone B receives 6.25% of the traffic." Defaults also verbatim: "With Application Load Balancers, cross-zone load balancing is always enabled at the load balancer level. At the target group level, cross-zone load balancing can be disabled. With Network Load Balancers and Gateway Load Balancers, cross-zone load balancing is disabled by default." I further confirmed the default independently from each guide's attribute list — NLB and GWLB both document `load_balancing.cross_zone.enabled` ... "The default is `false`".

What I cut: the "Inter-AZ data transfer" row. Two problems. (1) Evidence. The drafter supported a claim about NLB *and GWLB* charging with a quote lifted from a PrivateLink page. I fetched that page and two others; first-party text supports the NLB side only — create-endpoint-service.html: "Regional data transfer charges apply when you enable cross-zone load balancing" (NLB context), privatelink-share-your-services.html: "when you enable cross-zone load balancing for a Network Load Balancer, EC2 data transfer charges apply", and the NLB guide's ALB-target page: "You can disable cross-zone load balancing in both load balancers to minimize latency and avoid Regional data transfer charges." Neither the GWLB gu

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — A PrivateLink endpoint service must be fronted by an NLB (or a GWLB) — "expose one service to other accounts/VPCs" is an NLB trigger

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/vpc/latest/privatelink/privatelink-share-your-services.html

**Replaces (verbatim in note):**
```markdown
| Use it for | web apps, path/host routing, WebSockets, HTTP-aware | TCP/UDP, extreme perf, static IP, non-HTTP | inline firewalls/IDS/IPS appliances |
```

**Proposed:**
```markdown
| Use it for | web apps, path/host routing, WebSockets, HTTP-aware | TCP/UDP, extreme perf, static IP, non-HTTP, **fronting a PrivateLink endpoint service** | inline firewalls/IDS/IPS appliances (**also a valid endpoint-service front**) |
```

<details><summary>adversary reasoning</summary>

Verified from a page the drafter did not cite, which I fetched myself: privatelink-share-your-services.html — "As the service provider, you create a Network Load Balancer in your VPC as the service front end. You then select this load balancer when you create the VPC endpoint service configuration." I then fetched create-endpoint-service.html to check the GWLB half of the claim, because "NLB only" is the version most people carry and it would have been the easy thing to get wrong: "Endpoint services require either a Network Load Balancer or a Gateway Load Balancer." So the drafter was right to put the note in the GWLB cell too — narrowing it to NLB would have introduced an error.

I tried to refute on a technicality: the GWLB route is documented separately as "Access virtual appliances" and is consumed via a Gateway Load Balancer endpoint rather than an interface endpoint, so the two are not interchangeable in practice. But AWS's own sentence is unqualified, and the cell's parenthetical says "also a valid endpoint-service front", not "interchangeable" — accurate as written.

Internal consistency: this is a one-cell edit to the existing ALB/NLB/GWLB table and contradicts nothing else in the note. Exam relevance is real — "expose a service privately to another VPC/account" is a recurring SAA-C03 stem, and the note's NLB triggers currently stop at static IP and source-IP preservation, so the third trigger is a genuine gap rather than padding.

</details>

---

### [? · ? · 0 qid(s)] 04-alb-asg.md — PrivateLink as a third NLB trigger in the NLB-vs-ALB trap, plus why "restrict with security groups" is the wrong shape of answer, plus the ALB-behind-NLB escape hatch

**Verdict:** UPHELD  ·  **Citation:** https://raw.githubusercontent.com/hashicorp/terraform-provider-aws/main/website/docs/r/lb_target_group.html.markdown

**Replaces (verbatim in note):**
```markdown
> [!warning] Trap — NLB vs ALB for a static IP / source-IP
> "Need a static IP for the LB" or "must preserve client source IP with no app changes" → **NLB** (static IP/EIP per AZ, native source-IP preservation). ALB is DNS-only and needs `X-Forwarded-For`. Frequent distractor pairing.
```

**Proposed:**
```markdown
> [!warning] Trap — NLB vs ALB for a static IP / source-IP / PrivateLink
> "Need a static IP for the LB" or "must preserve client source IP with no app changes" → **NLB** (static IP/EIP per AZ, native source-IP preservation). ALB is DNS-only and needs `X-Forwarded-For`. Frequent distractor pairing. Third trigger, same pairing: **"expose one service to another VPC or account without exposing the rest of the VPC" → PrivateLink, and an endpoint service must be fronted by an NLB or a GWLB** — never an ALB directly. Security groups narrow *who may reach* something already reachable; they create no route, so "tighten the security group" is the wrong shape of answer. Need Layer 7 routing behind PrivateLink? Register the ALB as a target of the NLB (target group `target_type = "alb"`, protocol TCP, one ALB per target group).
```

<details><summary>adversary reasoning</summary>

Every factual clause checks out against pages I fetched. Endpoint service requires NLB or GWLB, never an ALB directly — create-endpoint-service.html: "Endpoint services require either a Network Load Balancer or a Gateway Load Balancer." The escape hatch — network/application-load-balancer-target.html: "You can use the layer 7 request-based routing feature of the Application Load Balancer in combination with features that the Network Load Balancer supports, such as endpoint services (AWS PrivateLink) and static IP addresses"; "You can only register one Application Load Balancer per target group"; and the target group must be `--target-type alb --protocol TCP` ("For **Protocol**, only TCP is allowed"). Because the draft names a Terraform argument, I checked the provider source rather than the registry (which serves a JS shell): `target_type = "alb"` is a documented value in the provider's own lb_target_group docs, with `port` and `vpc_id` required for it. So the Terraform spelling is right too.

The security-group sentence is reasoning, not a doc-citable limit, so I held it to the standard of "is it true": a security group filters traffic on a path that already exists; it creates no route between VPCs. Correct, and it is the right shape of correction for the distractor it names.

One thing I flag rather than refute: this and the table-cell edit write the same PrivateLink→NLB fact into two drillable artifacts, and §13.7 warns that a hand-written duplicate is how the retired cards drifted. I still uphold both, because the generator turns them into different drills — the table grid drills "which LB for which job", the trap drills "which stem phrase → which LB" — and the note already handles cross-zone the same way (Key-facts bullet plus trap). If only one is applied, keep th

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — IAM DB authentication has a database-side half: the instance switch (off by default) plus a DB user mapped to IAM inside the engine (AWSAuthenticationPlugin on MySQL/MariaDB, GRANT rds_iam on PostgreSQL)

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/UsingWithRDS.IAMDBAuth.Enabling.html

**Proposed:**
```markdown
| What you must turn on in AWS | nothing | **IAM database authentication on the instance/cluster** (`iam_database_authentication_enabled = true`) — **off by default** — *plus* an IAM policy granting `rds-db:connect` | a secret, plus `secretsmanager:GetSecretValue` |
| What you must do inside the database | create an ordinary password user | **map the DB user to IAM inside the engine** — `AWSAuthenticationPlugin` (MySQL/MariaDB) · `GRANT rds_iam` (PostgreSQL) | nothing special — an ordinary password user |
```

<details><summary>adversary reasoning</summary>

Facts all confirmed by my own fetches. Enabling.html: "By default, IAM database authentication is disabled on DB instances." DBAccounts.html: "CREATE USER 'jane_doe' IDENTIFIED WITH AWSAuthenticationPlugin AS 'RDS';" and "CREATE USER db_userx; GRANT rds_iam TO db_userx; ... You must grant the rds_iam role to use IAM authentication." So nothing here is false, and no existing table or callout in the note contradicts it.

Two reasons to narrow rather than uphold as written. (1) SCOPE: the note ALREADY states the IAM-mapping requirement in prose three times — line 122 ("a database user that must already exist and be mapped to IAM — AWSAuthenticationPlugin on MySQL, GRANT rds_iam TO <user> on PostgreSQL"), line 152 (Terraform map Notes column), and line 247. The drafter's rationale for restating it as a table row is legitimate under 13.7 (prose is never drilled), so the row earns its place — but the genuinely NEW fact is only "off by default," which the note nowhere states. (2) DRILLABILITY: the self-test generator blanks table CELLS and asks the human to reproduce them. A cell containing full DDL (`IDENTIFIED WITH AWSAuthenticationPlugin AS 'RDS'`, `CREATE USER db_userx; GRANT rds_iam TO db_userx;`) demands byte-exact SQL recall that is well outside SAA-C03 scope and outside this learner's stated preference (memory: teach mapping patterns, don't quiz exact strings). Compressing the cell to the plugin/role NAMES keeps the drill on the testable discrimination — "which engine uses which mechanism" — and drops the DBA trivia. Also changed "on the instance" to "on the instance/cluster" since the note covers Aurora and line 152 already says both.

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — An attached IAM role or a tightened security group is never the RDS authentication answer — the feature must be named and it has three required parts

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/UsingWithRDS.IAMDBAuth.Enabling.html

**Proposed:**
```markdown
> [!warning] Trap — an IAM role on the app is not, by itself, database authentication
> The distractors are **"attach an IAM role to the EC2 instance / Lambda function"** and **"restrict
> the security group to the app tier"**, offered *on their own*. A role is an **identity** control and
> a security group is a **network** control; neither authenticates anyone to a database. The role is
> genuinely needed — it is what carries the `rds-db:connect` policy — but it does nothing until the
> feature itself is switched on, and **the feature is off by default**. So the answer that earns the
> mark **names the feature**: *enable IAM database authentication*. The three parts it then requires
> are in the authentication comparison table above.
```

<details><summary>adversary reasoning</summary>

The underlying exam point is real and the "off by default" anchor is confirmed by my own fetch of Enabling.html. But the callout as drafted has two defects that make it unsafe to apply verbatim.

FIRST — IT CONTRADICTS THE NOTE'S OWN WORKED EXAMPLE. Line 253 tells the human to "attach a policy allowing rds-db:connect ... to the **instance profile role** the ASG's launch template already assigns," and closes "This is the canonical exam answer." The draft then says "'attach an IAM role to the EC2 instance / Lambda function' is an *identity* control ... **neither is an authentication mechanism**." A human revising under pressure hits both and cannot tell whether attaching a role is the right answer or the distractor. Worse, the draft is self-contradictory internally: its own part (2) says the `rds-db:connect` policy is "attached to the role," and then the closing sentence dismisses attaching a role. The defect is that the draft attacks roles per se, when the actual distractor is a role offered ALONE, without the feature being enabled.

SECOND — REDUNDANCY. This callout restates all three mechanics that the companion comparison-table rows now carry, plus the `rds-db:connect` prefix point that the existing "Trap — rds-db: vs rds:" callout (line 277) already owns. Per 13.7 that produces two independent drills of the same fact and two copies free to drift — the exact failure that retired the flashcards. The narrowed version keeps ONLY what a table cannot express (the distractor-elimination framing), points at the table for the mechanics, and drops the duplicated prefix material.

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — Encryption in transit on RDS (rds.force_ssl, static → reboot, plus the client-side RDS root CA bundle) versus TDE and KMS storage encryption, which are at rest only

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/UsingWithRDS.SSL.html

**Proposed:**
```markdown
> [!warning] Trap — TDE vs SSL/TLS: at rest vs in flight
> Asked for **in-flight** encryption on RDS, the distractor is **TDE** — a real, familiar-sounding
> encryption feature that is **at rest only**: it "automatically encrypts data before it is written
> to storage." Same for KMS `storage_encrypted`. Neither touches the wire.
> In flight is **SSL/TLS**, and RDS supports it on connections already — the knob does not *enable*
> encryption, it **requires** it and rejects plaintext connections. On **SQL Server** and
> **PostgreSQL** that knob is the **`rds.force_ssl` DB *parameter***: on SQL Server it defaults to
> `0` (off) and is **static**, so you set it in a custom **parameter group** and then **reboot**;
> RDS for PostgreSQL 15 and later already defaults it to `1`, 14 and older to `0`. (Other engines
> use a different parameter — check the engine's page.) Note the different knobs: TDE is an
> **option group** option, `rds.force_ssl` is a **parameter group** parameter.
> Server-certificate **verification** — downloading the RDS root CA bundle into the client's trust
> store — is a separate and **optional** step, and it guards against **spoofing**, not eavesdropping.
```

<details><summary>adversary reasoning</summary>

The TDE half is solid — I fetched Appendix.SQLServer.Options.TDE.html myself: "TDE automatically encrypts data before it is written to storage" and "specify the TDE option in an RDS option group." The rds.force_ssl defaults are solid too: SQL Server page says "By default, the rds.force_ssl parameter is set to 0 (off) ... is static, so after you change the value, you must reboot your DB instance"; PostgreSQL page says "default value is 1 (on) for RDS for PostgreSQL version 15 and later ... for ... 14 and older, the default value of this parameter is 0 (off)." All confirmed.

BUT THE CLIENT-SIDE CLAIM IS OVERSTATED, AND THE DRAFTER'S QUOTE IS CHERRY-PICKED. The draft frames in-flight encryption as having "a name on each side," with the CA bundle as the required client half. I fetched UsingWithRDS.SSL.html and its own lead sentence says the opposite: "**Optionally**, your SSL/TLS connection can perform server identity verification by validating the server certificate installed on your database. To require server identity verification, follow this general process: ... Download a certificate bundle." The trust store is for OPTIONAL server-identity verification (anti-spoofing), not a prerequisite for encrypting the wire. The drafter quoted a weaker line from further down the same page ("the database instance requires a trust certificate") that reads as mandatory in isolation. This is precisely the pattern I exist to catch — a real URL attached to a stronger claim than the page makes.

SECOND OVERSTATEMENT: the draft casts rds.force_ssl as how you get in-flight encryption. The PostgreSQL page I fetched says "By default, RDS for PostgreSQL uses and expects all clients to connect using SSL/TLS, but you can also require it." The parameter REQUIRES TLS and rejects plaintext; it do

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — Multi-AZ operational side-effects beyond failover: OS updates applied standby-first then promoted, automated backups taken from the standby so I/O is not suspended on the primary — but a DB engine version upgrade hits both instances at once

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_UpgradeDBInstance.Maintenance.html

**Proposed:**
```markdown
> [!warning] Trap — Multi-AZ only pays off when an AZ fails
> It pays off on ordinary Tuesdays too, and select-two questions lean on this. **OS patching goes
> standby-first:** RDS patches the **standby**, **promotes it to primary**, then patches the old
> primary, which becomes the new standby — so you take a short failover (AWS: "typically lasts less
> than a minute") instead of an outage. **Automated backups and DB snapshots are taken from the
> standby**, so I/O is **not suspended on the primary** during the backup window.
> Two limits worth holding: this is the **operating system** path — a **DB engine version** upgrade
> modifies **both** instances at once and the deployment is **unavailable** for the duration (RDS for
> SQL Server is the exception — rolling upgrades, outage only for a failover); and the standby is
> still **not readable**, which is the one thing Multi-AZ never buys you.
```

<details><summary>adversary reasoning</summary>

Core claims confirmed by my own fetch of the maintenance page: "Amazon RDS applies operating system updates by following these steps: 1. Perform maintenance on the standby. 2. Promote the standby to primary. 3. Perform maintenance on the old primary, which becomes the new standby." And the engine-upgrade sentence: "Amazon RDS modifies both primary and secondary DB instances at the same time ... both ... are unavailable during the upgrade." The not-readable limit is confirmed on Concepts.MultiAZSingleStandby.html: "You can't use a standby replica to serve read traffic." Backups-from-standby confirmed on aws.amazon.com/rds/faqs: "If you are running a Multi-AZ deployment, automated backups and DB Snapshots are simply taken from the standby to avoid I/O suspension on the primary."

ONE BLANKET CLAIM IS FALSE AS WRITTEN, AND THE REFUTATION IS ON THE DRAFTER'S OWN CITED PAGE. The draft says a DB engine version upgrade makes "the deployment unavailable for the duration" with no exception. Four sentences below the passage the drafter quoted, the same page states: "If you upgrade an RDS for SQL Server or RDS Custom for SQL Server DB instance in a Multi-AZ deployment, then Amazon RDS performs rolling upgrades, so you have an outage only for the duration of a failover." The drafter stopped reading at the quote that supported the claim. Added as a parenthetical hedge — one clause, not a new block.

SECOND, SMALLER FIX: the draft says you "take a failover instead of an outage" without sizing it. The same page quantifies it — "a short Multi-AZ failover is required to apply the patches to the primary DB instance. This failover typically lasts less than a minute" — so I state it, which also keeps it consistent with the note's existing ~60–120s failover figure at line 35 rather than imp

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — The note's closing Multi-AZ clause "you are buying availability, and only availability" is too strong once standby-first patching and backup-from-standby are documented

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.MultiAZSingleStandby.html

**Replaces (verbatim in note):**
```markdown
That feels wasteful, but it is the deal: the standby is kept synchronously identical so that failover loses nothing. Multi-AZ **doubles the cost and is not Free-Tier** — you are buying availability, and only availability.
```

**Proposed:**
```markdown
That feels wasteful, but it is the deal: the standby is kept synchronously identical so that failover loses nothing. Multi-AZ **doubles the cost and is not Free-Tier** — and what you are buying is availability, **never read capacity**. (It does quietly buy two operational wins as well — see the Multi-AZ maintenance trap below.)
```

<details><summary>adversary reasoning</summary>

I tried to refute this one as unnecessary fiddling with correct prose, and it survives — but only as a dependent edit, exactly as the drafter flagged.

The existing clause is not factually wrong: read the paragraph and "availability, and only availability" plainly means "not read capacity," which AWS states outright on the page I fetched — "The high availability option isn't a scaling solution for read-only scenarios. You can't use a standby replica to serve read traffic." So on its own this is a style change and I would refuse it.

What makes it necessary is the Multi-AZ maintenance trap landing in the same note. Once the note documents that Multi-AZ also buys standby-first OS patching and backup-from-standby (both verified — the maintenance page's three-step OS sequence and the RDS FAQ's "automated backups and DB Snapshots are simply taken from the standby"), a blanket "only availability" at line 39 directly contradicts a callout roughly 240 lines later. An internally inconsistent note is worse than an incomplete one, and this learner's recorded failure mode is discrimination — a flat contradiction between two passages is the worst possible input for that.

The replacement preserves both load-bearing facts (doubles cost, not Free-Tier; never read capacity), sharpens "only availability" into the precise claim AWS actually supports ("never read capacity"), and forward-references the trap instead of restating it, so no fact is written twice.

APPLY ONLY TOGETHER WITH THE MULTI-AZ TRAP ABOVE. If that callout is not applied, skip this one — the cross-reference would dangle and the original sentence is fine as it stands.

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — Cross-Region read replica offered as the DR answer, and the non-existent "multi-Region Aurora cluster" distractor

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Concepts.AuroraHighAvailability.html

**Proposed:**
```markdown
> [!warning] Trap — a cross-Region read replica offered as the DR answer
> Both options replicate continuously and both can be promoted, so a cross-Region
> read replica *looks* like a valid answer to "cross-Region DR". Three things
> eliminate it. **Promotion is manual** — you run `promote-read-replica-db-cluster`
> yourself, the instances reboot, and it takes several minutes *after* someone
> decides to do it; Global Database has a **managed** failover and a managed
> switchover, which is what "MOST operationally efficient" is pointing at. The
> replication is **binlog replication run by the database engine**, which you have
> to configure and manage (`binlog_format`), against Global Database's
> **storage-layer** replication at **typically under a second**. And it only exists
> on **Aurora MySQL** at all — AWS: "Aurora PostgreSQL doesn't support cross-Region
> Aurora Replicas." Any stated requirement near "RPO in seconds, RTO in minutes" is
> Global Database's published envelope, not a read replica's.
>
> The third distractor in this family is **"a multi-Region Aurora cluster"**. There
> is no such thing. An Aurora DB cluster lives in **one Region** — AWS: "Aurora
> stores copies of the data in a DB cluster across multiple Availability Zones in a
> **single AWS Region**." Multi-Region means *several* clusters joined into a global
> database, never one cluster stretched across Regions.
```

<details><summary>adversary reasoning</summary>

I tried to refute the 'no such thing as a multi-Region Aurora cluster' claim and could not — it holds on two independent first-party sentences I fetched: 'Aurora stores copies of the data in a DB cluster across multiple Availability Zones in a single AWS Region' and 'The cluster volume spans multiple Availability Zones (AZs) in a single AWS Region.' The global-database page draws the same line the draft does — the GLOBAL DATABASE spans Regions ('you set up multiple Aurora DB clusters that span multiple AWS Regions'), the DB cluster does not. Managed vs manual promotion also verifies ('Aurora Global Database supports two different operations ... switchover and ... failover' vs the CLI `promote-read-replica-db-cluster`). Only narrowing: same engine-scope hole as draft 1. The trap argues against the cross-Region read replica on mechanism grounds without saying the option does not exist at all on Aurora PostgreSQL — which is both true and a stronger elimination, so adding it improves the trap rather than weakening it.

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — An Aurora Replica is the read-scaling mechanism and the automatic failover target at the same time

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/multi-az-db-clusters-concepts.html

**Proposed:**
```markdown
| Replica doubles as failover target | **No** — a Multi-AZ **DB instance** standby answers no queries (the separate Multi-AZ **DB cluster** deployment is the exception — readable readers that *are* automatic failover targets) | **Yes** — an Aurora Replica is the read-scaler **and** the promotion target |
```

<details><summary>adversary reasoning</summary>

The Aurora half is solid — I fetched Aurora.Replication.html: 'Aurora Replicas have two main purposes. You can issue queries to them to scale the read operations ... Aurora Replicas also help to increase availability. If the writer instance in a cluster becomes unavailable, Aurora automatically promotes one of the reader instances.' It is also a documented weak spot in this very note (line 305, trainer-sourced miss marked 'sure'), and turning the existing line-68 prose into a table row is the right call because the self-test generator only drills tables and trap callouts. But the RDS cell as drafted — a flat 'No — Multi-AZ runs a separate standby that answers no queries' — CONTRADICTS THIS NOTE'S OWN LINE 37, which already carves out the exception, and contradicts AWS: a Multi-AZ DB cluster deployment has 'two readable replica DB instances' and 'Reader DB instances act as automatic failover targets and also serve read traffic.' An internally inconsistent note is the failure mode I am here to prevent, so the qualifier goes in.

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — "Standby" is RDS vocabulary with no Aurora equivalent

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.MultiAZSingleStandby.html

**Proposed:**
```markdown
> [!warning] Trap — the word "standby" in an Aurora answer option
> **"Standby" is RDS vocabulary and has no Aurora equivalent.** An RDS Multi-AZ
> **DB instance** deployment provisions "a synchronous standby replica in a different
> Availability Zone" that you explicitly **cannot** read from. Aurora has no such
> instance: AWS lists exactly two ways a cluster gets a new writer — "**by promoting
> an existing Aurora Replica**" or "by creating a new primary instance" — and there
> is no third, unreadable, standby-shaped option. So an option saying "create a
> **standby** Aurora instance" is wrong on the noun alone; the right answer says
> **Replica**.
>
> The same word-level tell answers "improve the availability of a single-instance
> Aurora cluster". You do not add a standby or a Multi-AZ second copy — you **add
> Aurora Replicas**, because the Replica is simultaneously the read-scaler and the
> thing that gets promoted. AWS states it as the availability recommendation
> outright: "To increase the availability of your DB cluster, we recommend that you
> create at least one or more Aurora Replicas in two or more different Availability
> Zones."
```

<details><summary>adversary reasoning</summary>

I attacked the word-level claim directly by checking whether AWS ever calls a readable RDS instance a 'standby' — it does not: the Multi-AZ DB cluster page consistently says 'readable replica DB instances' / 'reader DB instances', never standby. So 'standby' really is reserved for the unreadable single-standby deployment, and the Aurora HA page I fetched lists exactly two failover paths — 'By promoting an existing Aurora Replica to the new primary instance' / 'By creating a new primary instance' — with no third standby-shaped option. The availability recommendation verifies verbatim too: 'To increase the availability of your DB cluster, we recommend that you create at least one or more Aurora Replicas in two or more different Availability Zones.' Narrowing is one word: the draft says 'An RDS Multi-AZ deployment provisions a synchronous standby replica ... that you explicitly cannot read from', but AWS's sentence is scoped — 'In a Multi-AZ DB instance deployment ...'. Unqualified, it collides with line 37 of this note. Adding 'DB instance' makes it more faithful to the quote the draft itself cites.

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — Aurora failover promotion order — priority tiers 0–15, tie-breaks, five-attempt rule, and failover timing figures

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Concepts.AuroraHighAvailability.html

**Proposed:**
```markdown
### Aurora failover — which instance gets promoted

|   | Rule |
|---|---|
| Priority range | **0–15**, set per Aurora Replica — **0 is the highest** priority, 15 the lowest |
| Who wins | the Replica with the **highest priority**, i.e. the **lowest tier number** |
| How long, with a Replica | typically **under 60 seconds**, often under 30 |
| How long, with no Replica | primary is **recreated in the same AZ**, typically **under 10 minutes** |

Changing a replica's priority does not itself trigger a failover. (Ties are broken by largest instance size, then arbitrarily within the tier; after five unsuccessful failover attempts promotion tiers are ignored — true, but below SAA-C03 depth.)
```

<details><summary>adversary reasoning</summary>

Every single cell is factually correct — I fetched the page and all five quotes appear verbatim, including the easily-garbled ones ('Priorities range from 0 for the highest priority to 15 for the lowest', 'Amazon RDS promotes the replica that is largest in size', 'promotes an arbitrary replica in the same promotion tier', 'After five unsuccessful failover attempts, promotion tiers are no longer considered', 'typically restored in less than 60 seconds, and often less than 30 seconds', 'typically takes less than 10 minutes'). Nothing to refute on truth. The objection is RELEVANCE, which §13.11 makes a real standard: the tie-break-by-size rule, the arbitrary-within-tier rule and the five-attempt rule are RDS-specialty operational depth, not SAA-C03 material, and because the generator turns every table row into a drill, shipping them spends the human's recall budget on trivia. The rows that earn their place are the ones that carry exam signal: the 0-is-highest inversion (a classic reversal) and the <60s-with-a-replica vs <10min-without contrast, which is the quantitative payoff for the 'always keep a Replica' argument in the two traps above. Trimmed table below; the dropped rows are correct and can live in the note if the human wants them, just not as drilled rows.

</details>

---

### [? · ? · 0 qid(s)] 07-rds-aurora.md — Failover on a single-instance Aurora cluster recreates the primary in the SAME AZ; whole-AZ loss requires manually creating instances elsewhere

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Concepts.AuroraHighAvailability.html

**Proposed:**
```markdown

```

<details><summary>adversary reasoning</summary>

I tried to refute the counter-intuitive part — 'recreated in the same AZ' — because it is the kind of claim a drafter invents. It is verbatim AWS: 'If the DB cluster doesn't contain any Aurora Replicas, then the primary instance is recreated in the same AZ during a failure event. A failure event results in an interruption during which read and write operations fail with an exception. Service is restored when the new primary instance is created, which typically takes less than 10 minutes.' The sequel verifies too: 'If your provisioned or Aurora serverless cluster only contains a single DB instance, or if the primary instance and all reader instances are in the same AZ, make sure to manually create one or more new DB instances in another AZ.' The cluster-volume-survives framing is supported by 'The cluster volume spans multiple Availability Zones (AZs) in a single AWS Region, and each Availability Zone contains a copy of the cluster volume data.' No contradiction with the rest of the note. Exam-relevant: it is the mechanism behind the standard 'single-instance Aurora cluster, improve availability' question and it supplies the *why* for the recommendation in the standby trap. One editorial note for the human: this trap and the 'standby' trap both close on 'therefore add a Replica in a second AZ' — consider trimming the last sentence of one of them so the pair does not read as repetition.

</details>

---

### [? · ? · 0 qid(s)] 02-ec2.md — Trap callout: instance store wins on raw I/O; io2 wins only when the data must survive a restart.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-lifecycle.html

**Proposed:**
```markdown
> [!warning] Trap — "maximum possible I/O performance" means provisioned-IOPS EBS
> No — that phrase points at **instance store**. Its disks are physically attached to the host, so
> there is no network hop, and on the same instance it beats EBS on raw IOPS and latency by roughly
> an order of magnitude (see the EBS vs Instance Store table). `io2 Block Express` at 256,000 IOPS is
> the ceiling for a *single EBS volume*, not for EC2 — and the instance's own EBS connection caps
> that again. The price of instance store is durability: the data is erased on **stop, hibernate and
> terminate**, so the scenario has to tell you it is scratch, cache, or regeneratable. If it says
> both "maximum I/O" *and* "must survive a restart", it flips back to `io2`.
```

<details><summary>adversary reasoning</summary>

Every factual component verified. The 'dies on stop, hibernate and terminate' claim I confirmed from a page the drafter did not cite — the 'Differences between instance states' table on ec2-instance-lifecycle.html, row 'Instance store volumes': Reboot 'The data is preserved' | Stop/start 'The data is erased' | Hibernate 'The data is erased' | Terminate 'The data is erased'. Earns its place despite the note already carrying weak-spot line 410 on the same discriminator, because §13.7 only drills comparison tables and trap callouts — a weak-spot bullet is never turned into a self-test question, and this is a distinction the human has already measurably missed on a mock.

NARROWED for the same block-size problem as verdict 1: repeating '7,200,000 vs 240,000' inside the trap re-asserts a non-apples-to-apples comparison in the one artifact the self-test lifts VERBATIM as an answer. Numbers belong in the table row where the block sizes are stated; the trap should carry the mechanism. Also trimmed the closing sentence, which restated the opening.

</details>

---

### [? · ? · 0 qid(s)] 02-ec2.md — Hibernation as a third lifecycle state — rewrite the Stop vs Terminate table as Stop vs Hibernate vs Terminate.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-lifecycle.html

**Replaces (verbatim in note):**
```markdown
### Stop vs Terminate

|   | Stop | Terminate |
|---|---|---|
| EBS root volume | Preserved | Deleted (default `delete_on_termination=true`) |
| Additional EBS volumes | Preserved | Preserved (default `delete_on_termination=false`) |
| Instance store data | **Lost** | **Lost** |
| Instance ID | Preserved | Released, can never be reused |
| Private IP | Preserved | Released |
| Auto-assigned public IP | **Released** | Released |
| Elastic IP (if associated) | Stays associated | Disassociated; EIP retained in your account |
| AMI ID metadata | Preserved | Preserved |
| Physical host | May change on start | N/A |
| Billing | Compute stops; EBS still bills | Stops entirely |
```

**Proposed:**
```markdown
### Stop vs Hibernate vs Terminate

|   | Stop | Hibernate | Terminate |
|---|---|---|---|
| **RAM contents** | **Erased** | **Saved** — written to the EBS root volume, reloaded on start, processes resume | **Erased** |
| EBS root volume | Preserved | Preserved — and holds the saved RAM | Deleted (default `delete_on_termination=true`) |
| Additional EBS volumes | Preserved | Preserved, still attached | Preserved (default `delete_on_termination=false`) |
| Instance store data | **Lost** | **Lost** | **Lost** |
| Instance ID | Preserved | Preserved | Released, can never be reused |
| Private IP | Preserved | Preserved | Released |
| Auto-assigned public IP | **Released** | **Released** — new one on start | Released |
| Elastic IP (if associated) | Stays associated | Stays associated | Disassociated; EIP retained in your account |
| AMI ID metadata | Preserved | Preserved | Preserved |
| Physical host | May change on start | May change on start | N/A |
| Modify it while off (e.g. instance type)? | Yes | **No** | N/A |
| Billing | Compute stops; EBS still bills | No instance charge while `stopped` — **but you are billed through the `stopping` state**, unlike a plain stop. EBS bills, including the space holding the RAM | Stops entirely |
| Turning it on | Always available | **Enable at launch only** — encrypted EBS root sized to hold the RAM, Linux RAM < 150 GiB (Windows ≤ 16 GiB), supported family, not bare metal, max 60 days hibernated. You can't hibernate an instance you placed in an Auto Scaling group (ASG **warm pools** are the exception — the ASG hibernates those itself) | Always available |
```

<details><summary>adversary reasoning</summary>

Core content verified, but I found THREE problems, one of which is a claim the drafter's own cited page contradicts.

(a) REFUTED SUB-CLAIM — the Physical host asymmetry. The draft writes 'May change on start' for Stop but 'Usually changes on start' for Hibernate, implying hibernate is likelier to migrate. AWS uses IDENTICAL wording for both. From the lifecycle-differences table I fetched myself: Stop/start → 'The instance is moved to a new host computer (though in some cases, it remains on the current host).' Hibernate → the same sentence, word for word. The invented asymmetry is exactly the kind of plausible-sounding detail that survives into an exam answer. Made both cells identical, and left the existing Stop cell untouched.

(b) MISSING NUANCE, and it is the one genuinely hibernate-specific billing fact. The draft's Billing cell says 'no instance-usage charge' — but AWS explicitly singles hibernate out: 'You are not charged for usage for a hibernated instance when it is in the stopped state, but you are charged while it is in the stopping state, unlike when you stop an instance without hibernating it.' The same page's state table repeats it: for stopping, 'Not billed — If you hibernate an instance, you're billed while the instance is in the stopping state.' Added.

(c) OVERSTATED — 'not in an Auto Scaling group' as an absolute. The hibernate limitations page does say 'You can't hibernate an instance that is in an Auto Scaling group', but ASG warm pools hibernate instances that are by definition in an ASG. From the warm pools page (which I fetched; the drafter did not cite it): 'You can keep instances in the warm pool in one of three states: Stopped, Running, or Hibernated... you can keep instances in a Hibernated state to stop instances without deleting their memor

</details>

---

### [MISSING · high · 1 qid(s)] 13-cost-optimization.md — Vertical vs horizontal scaling as an axis, with Compute Optimizer as the vertical tool and ASG+ELB as the horizontal one (and only the horizontal one improving availability)

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/autoscaling/ec2/userguide/what-is-amazon-ec2-auto-scaling.html

**Proposed:**
```markdown
### Vertical vs horizontal scaling

|   | **Vertical — scale up/down** | **Horizontal — scale out/in** |
|---|---|---|
| What changes | **the size or class of one resource** | **the number of resources** |
| How the change is made | **stop the instance, select a different size or class, start it** | **EC2 Auto Scaling launches or terminates instances**; ELB distributes demand across them ([[04-alb-asg]]) |
| The AWS tool that points at it | **Compute Optimizer** — rightsizing recommendations from **14 days** of CloudWatch metrics | **CloudWatch metrics and alarms** driving an Auto Scaling policy |
| Improves availability? | ❌ no added redundancy, and the **stop/start is downtime** | ✅ ASG **balances instances across AZs** and **replaces impaired instances** automatically |
```

<details><summary>adversary reasoning</summary>

Content verified — I fetched all three sources myself and every quoted string is verbatim-accurate. Well-Architected COST09-BP03: 'You can use APIs to scale the size of resources within an environment (vertical scaling). For example, you could scale up a production workload by changing the instance size or class. This can be achieved by stopping and starting the instance and selecting the different instance size or class' and 'Elastic Load Balancing ... helps you scale by distributing demand across multiple resources.' EC2 Auto Scaling: 'balances your instances evenly across the Availability Zones ... provides high availability and resiliency' and 'replaces terminated or impaired instances.' Compute Optimizer: 'rightsizing recommendations and identify idle resources' from 'Amazon CloudWatch for the last 14 days.' I also checked the vault — no other note has a vertical/horizontal comparison table ([[04-alb-asg]] teaches horizontal scaling but never draws the axis), so this creates no duplicate source of truth.

Two narrowings:

(a) DROPPED the fourth row, 'Answers "CPU is pegged at 100%"' — both cells were ✅. A row where both columns agree carries zero discriminating value, and once `_selftest.py` blanks the cells the drill becomes 'reproduce ✅ and ✅', which is noise. This human's measured failure mode is discrimination, so a non-discriminating row is actively counterproductive. Replaced with 'How the change is made', which is cited and genuinely separates the two.

(b) SOFTENED 'still one instance'. Vertical scaling is about the size of a resource; it does not assert that only one instance exists, and a fleet can be vertically scaled. The cited, defensible claim is the one I wrote: no added redundancy, and the resize needs a stop/start. Also relabelled the tool row — Co

</details>

---

### [? · ? · 0 qid(s)] 13-cost-optimization.md — AWS Trusted Advisor is advisory only — recommendations, no access grant, no billing consolidation; six check categories; full checks gated behind a paid Support plan

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/consolidated-billing.html

**Proposed:**
```markdown
> [!warning] Trap — Trusted Advisor as a governance or billing answer
> Trusted Advisor **inspects your environment and makes recommendations** across six categories — cost optimization, performance, security, fault tolerance, service limits, operational excellence. Recommendations are the whole of it: it grants nobody access, enforces nothing, and combines nobody's bill. A scenario wanting **central cost oversight and guardrails across many accounts** is **AWS Organizations** — **consolidated billing** for the single bill and shared volume/RI/SP discounts, and **SCPs** for the permission ceiling that divisions can't escape ([[01-iam-advanced]]). Trusted Advisor answers neither half. Also: **all checks require a higher-tier paid Support plan** — currently **Business Support+, Enterprise, or Unified Operations**; **Basic and Developer** get the whole **Service limits** category plus a short list of Security and Fault tolerance checks. (⚠️ verified 2026-09-12 — AWS is consolidating Support plan names; Developer, Business and Enterprise On-Ramp are discontinued 1 Jan 2027.)
```

<details><summary>adversary reasoning</summary>

The factual core verified cleanly against docs I fetched. `awssupport/latest/user/trusted-advisor.html`: 'Trusted Advisor inspects your AWS environment, and then makes recommendations...' and the Basic-plan limitation verbatim as quoted. `trusted-advisor-check-reference.html` Topics list is exactly the six categories. Real gap too: `services:` in the frontmatter claims TrustedAdvisor but the body never mentions it once — I grepped the whole vault and Trusted Advisor appears in zero notes.

Two narrowings:

(a) 'the full check set needs a paid Support plan' IS IMPRECISE AND MISLEADS. Developer Support is a paid plan and does *not* get the full set. The check reference is explicit: 'If you have a Basic Support **or Developer Support** plan, you can use the Trusted Advisor console to access all checks in the Service limits category and the following checks...' Named the qualifying plans instead. Added a dated caution because the docs now carry end-of-support banners: Developer, Business and Enterprise On-Ramp are discontinued 1 Jan 2027 and the gating plans are now 'Business Support+, Enterprise, or Unified Operations' — the exam almost certainly still says 'Business and Enterprise', so this fact will drift and must be dated per §13.10.

(b) REWROTE THE GOVERNANCE HALF. The draft answered 'central IT governance ... while divisions keep autonomy' with 'consolidated billing plus cross-account IAM roles'. Cross-account roles give central IT *access*; they are not a governance guardrail, and this framing contradicts the vault's own teaching — [[01-iam-advanced]] line 28: 'Service Control Policies are how you set a rule that nobody inside them can escape — not even their own administrator.' AWS agrees: 'SCPs offer central control over the maximum available permissions for the I

</details>

---

### [? · ? · 0 qid(s)] 13-cost-optimization.md — Trusted Advisor row in the cost-tools reference table

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/awssupport/latest/user/trusted-advisor-check-reference.html

**Proposed:**
```markdown
| **Trusted Advisor** | "am I following best practice?" — recommendations only, six categories; all checks need Business Support+ / Enterprise / Unified Operations |
```

<details><summary>adversary reasoning</summary>

Verified from the two Trusted Advisor pages I fetched (definition and the six-item Topics list). The drafter's mechanical reasoning is right: `_selftest.py` skips tables under 3 columns, so `### The cost tools` is reference only and this row will not become a drill — the trap above is what teaches it. It also makes the `services: [... TrustedAdvisor]` frontmatter honest, which matters because the trainer's `notes.ts` joins on that field.

Narrowed only for the same precision fix as draft 4: 'a paid Support plan' is wrong for Developer Support, so the qualifying plans are named.

ONE CONSISTENCY FLAG FOR THE HUMAN, not a blocker. Line 124 opens with 'Five, and the exam mostly wants you to pick the right one for a described job' — that five counts the prose bullets (Cost Explorer, Budgets, Anomaly Detection, tags/categories, Compute Optimizer). The `### The cost tools` table already carries six rows because it also lists Cost Optimization Hub, which the prose never mentions. This row makes the table seven against a prose 'five'. Nothing is false, but if the human is touching the section anyway, either add Trusted Advisor to the prose list (and fix the count) or leave the count alone deliberately.

</details>

---

### [? · ? · 0 qid(s)] 05-vpc-security.md — Flow-log attachment level: ENI vs subnet vs VPC — comparison table

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/vpc/latest/userguide/flow-logs-basics.html

**Proposed:**
```markdown
### Which flow-log level to attach

|   | VPC | Subnet | ENI |
|---|---|---|---|
| What is monitored | **every** network interface in the VPC | **every** network interface in that subnet — including ones you did not mean to log | **exactly one** network interface |
| Picks up resources created later | Yes (new ENIs get a log stream automatically) | Yes, if launched into that subnet | No — it is bound to that one ENI |
| Reach for it when | blanket coverage / audit baseline across the whole VPC | the question is about a **subnet's** traffic as a whole | the question **names one resource** — an ELB, a NAT gateway, one instance |
| Noise level | highest | high | none — only the target resource |
```

<details><summary>adversary reasoning</summary>

I fetched flow-logs-basics.html myself (not relying on the drafter's quotes) and every cell in the table is supported verbatim: 'You can create a flow log for a VPC, a subnet, or a network interface. If you create a flow log for a subnet or VPC, each network interface in that subnet or VPC is monitored.' and 'If you launch an instance into your subnet after creating a flow log for your subnet or VPC, we create a log stream (for CloudWatch Logs) or log file object (for Amazon S3) for the new network interface.' The ENI row ('exactly one', 'No — bound to that one ENI') follows directly from the same sentence. I tried to refute it three ways and failed: (a) I checked flow-logs-limitations.html for a caveat that would break 'VPC level = every ENI' — the only scoping limitation is peered VPCs in other accounts, which does not touch the claim; (b) I checked the rest of 05-vpc-security.md for contradiction — line 100 and the Key-facts line 176 already say 'VPC, subnet or ENI level' but nowhere say what each level covers or how to choose, so this is a genuine gap, not a restatement; (c) placement is clean — the '## Comparisons' section currently holds two sub-tables ('Security Group vs Network ACL', 'Where each layer sits') and neither overlaps. The 'Noise level' row is editorial rather than doc-sourced, but it is a judgement about volume that follows from the monitored-interface counts, not a factual claim about AWS behaviour. Exam-relevant: level selection is a recurring scenario shape ('capture traffic for one instance / one NAT gateway'), and the note currently generates zero drills on it.

</details>

---

### [? · ? · 0 qid(s)] 05-vpc-security.md — ELB has an ENI in each enabled subnet; flow logs attach to service-created ENIs, so 'capture the ELB's traffic' means an ENI-level flow log

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/elasticloadbalancing/latest/application/application-load-balancers.html

**Proposed:**
```markdown
> [!warning] Trap — "to capture one resource's traffic, put the flow log on its subnet"
> A subnet-level flow log monitors **every** network interface in that subnet, so the resource you care about arrives buried in everything else's. Flow logs attach directly to interfaces **created by other AWS services** — **Elastic Load Balancing, RDS, ElastiCache, Redshift, WorkSpaces, NAT gateways, transit gateways** — so when a question names one resource, the ENI-level flow log is the answer. The caveat the exam glosses over: a flow log covers **exactly one interface**, and ELB creates interfaces **in every subnet you enabled it in** (an ALB requires at least two AZ subnets), so "log the ALB" in real life means one flow log per interface — or the subnet-level log after all.
```

<details><summary>adversary reasoning</summary>

Both underlying facts check out against pages I fetched myself. flow-logs-basics.html: 'You can create flow logs for network interfaces that are created by other AWS services, such as: + Elastic Load Balancing + Amazon RDS + Amazon ElastiCache + Amazon Redshift + Amazon WorkSpaces + NAT gateways + Transit gateways'. application-load-balancers.html, under 'Subnets for your load balancer': 'Elastic Load Balancing creates network interfaces in the subnets where you configured your load balancer.' But the draft overstates the conclusion in a way that would mislead. Note the plural in the ALB doc — interfaceS in the subnetS. An ALB must span at least two AZ subnets ('You must select at least two Availability Zone subnets'), and a flow log for a network interface covers *exactly one* interface. So 'the answer is the ENI-level flow log' (singular) does not in fact capture an ELB's traffic — you need one flow log per interface, and the same doc notes the ALB needs 'at least eight free IP addresses per subnet ... to allow the load balancer to scale out', so its addressing in a subnet is not a fixed single thing. A human who memorises the draft as written would answer a 'how do I capture all traffic to my ALB' question wrongly. Second problem: as drafted this trap is largely a prose restatement of the table in draft 1, and §13 of CLAUDE.md explicitly warns against writing the same fact twice in the same note. I have rewritten it to carry only what the table does not — the service-created-ENI list, which is the genuinely testable and additive fact — plus the per-interface caveat. I also dropped the 'ELB creates a network interface' singular phrasing in favour of the doc's plural. One first-party detail I verified but deliberately left out to keep the note lean: 'Regardless of the 

</details>

---

### [? · ? · 0 qid(s)] 05-vpc-core.md — The 'Create a VPC' console↔Terraform map row gives an incomplete reason for the DNS attributes (interface endpoints only) and omits the private-hosted-zone requirement

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/vpc/latest/privatelink/privatelink-access-aws-services.html

**Replaces (verbatim in note):**
```markdown
| Create a VPC | `aws_vpc` | `cidr_block`; set `enable_dns_hostnames = true` + `enable_dns_support = true` (public DNS + required for interface endpoints). |
```

**Proposed:**
```markdown
| Create a VPC | `aws_vpc` | `cidr_block`; `enable_dns_support = true` (Amazon DNS resolution — default **on**) + `enable_dns_hostnames = true` (public DNS names — default **off**). Both are required for a Route 53 **private hosted zone** and for **private DNS** on interface endpoints. |
```

<details><summary>adversary reasoning</summary>

The drafter is right that the existing row is imprecise, and I confirmed it from a page the drafter never cited. Line 133 currently reads '(public DNS + required for interface endpoints)'. privatelink-access-aws-services.html, which I fetched, shows that is wrong as stated: 'If you enable private DNS for your interface VPC endpoint, and your VPC has both DNS hostnames and DNS resolution enabled, we create a hidden, AWS-managed private hosted zone for you' — the attributes gate *private DNS*, not the endpoint. The same page shows an endpoint without private DNS is perfectly usable through its regional and zonal names ('{endpoint_id}.{service_id}.{region}.vpce.amazonaws.com'). So the note as written could lead the human to answer 'enable DNS hostnames' to a question where the endpoint itself is the issue, or to think an interface endpoint is impossible without it. The private-hosted-zone half is likewise confirmed on AmazonDNS-concepts.html ('If you use custom DNS domain names defined in a private hosted zone ... you must set both ... to true'). The replaces_text matches line 133 character-for-character, so the edit is safe to apply mechanically. I narrowed the replacement text for two reasons: the drafter's version repeats 'the **only two** DNS attributes a VPC has' — the exact claim the trap in the previous verdict already carries, and duplicating a fact inside one note is the pattern §13 says produces drift; and a lookup-table cell should stay scannable. My version keeps both corrections (private DNS, private hosted zone) and adds the two defaults, which is the part of the row that actually helps when reading someone else's HCL. Verdict is NARROWED rather than UPHELD only because of the wording; the substance of the correction stands.

</details>

---

### [? · ? · 0 qid(s)] 11-cloudfront.md — CloudFront custom error responses serve a static page while the origin is failing; supported status codes, the assumed 504, and the 10-second default error caching.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/custom-error-pages-expiration.html

**Proposed:**
```markdown
> [!warning] Trap — "serve a static page while the origin is failing"
> If the site is already behind CloudFront, the low-overhead answer is a **custom error response** on the distribution, not a second stack. CloudFront can return your own object for **400, 403, 404, 405, 414, 416** and **500, 501, 502, 503, 504** — including the **504 CloudFront assumes itself** when the origin stops responding — and that object can live in a different bucket from the content. Errors are cached for a **default 10 seconds** (`Error Caching Minimum TTL`), so the real page comes back on its own. Two edges: if the custom error object is missing, the viewer gets a **404** instead of the origin's status code; and a 503 caused by CloudFront's own `Capacity Exceeded`/`Limit Exceeded` skips your page entirely. Terraform: a `custom_error_response` block inside `aws_cloudfront_distribution`. If there is **no** CloudFront in front, [[10-route53]] failover to an S3 website bucket holding a "temporarily unavailable" page is AWS's documented pattern — it just costs a health check and a TTL wait.
```

<details><summary>adversary reasoning</summary>

Every CloudFront fact in the draft is verified first-hand: the status-code list ('400, 403, 404, 405, 414, 416' and '500, 501, 502, 503, 504'), the assumed 504 ('CloudFront doesn't get a response from your origin server for a certain period of time and assumes a 504 status code (Gateway Timeout)') — both on creating-custom-error-pages.html; the default caching ('CloudFront caches error responses for a default duration of 10 seconds', custom-error-pages-expiration.html, setting named 'Error Caching Minimum TTL'); the separate-bucket case (GeneratingCustomErrorResponses.html); and the Terraform argument 'custom_error_response (Optional) - One or more custom error response elements' plus 'error_caching_min_ttl' / 'response_page_path' in the provider source (raw.githubusercontent.com/hashicorp/terraform-provider-aws .../cloudfront_distribution.html.markdown). What must be cut is the framing clause 'not [[10-route53]] failover to an S3 site' — AWS documents DNS failover to an S3 'temporarily unavailable' bucket as a supported pattern, so asserting it is the wrong answer teaches a false either/or. I also added two edges AWS states explicitly and the draft omitted, both of which bite in practice: a missing custom error object makes CloudFront return 404 to the viewer instead of the origin's code, and a 503 from CloudFront's own Capacity Exceeded / Limit Exceeded bypasses the custom page. No conflict with the existing traps in 11-cloudfront.md (the OAC trap at line 222 already mentions custom error pages in passing, consistently).

</details>

---

### [? · ? · 0 qid(s)] 10-route53.md — A private hosted zone requires both VPC DNS attributes set to true: enableDnsSupport and enableDnsHostnames.

**Verdict:** UPHELD  ·  **Citation:** https://docs.aws.amazon.com/vpc/latest/userguide/AmazonDNS-concepts.html

**Proposed:**
```markdown
| VPC DNS attributes | n/a | **both** `enableDnsSupport` **and** `enableDnsHostnames` set to `true` (Terraform: `enable_dns_support` / `enable_dns_hostnames`) |
```

<details><summary>adversary reasoning</summary>

Verified independently, and deliberately from a different first-party page than the drafter used (they cited the Route 53 considerations page; I went to the VPC User Guide). Amazon VPC User Guide, 'Understanding Amazon DNS' states twice: 'If you use custom DNS domain names defined in a private hosted zone in Amazon Route 53, or use private DNS with interface VPC endpoints (AWS PrivateLink), you must set both the `enableDnsHostnames` and `enableDnsSupport` attributes to `true`', and repeats it as an Important callout under the 'Private hosted zones' heading. Same page confirms the two attribute definitions and their defaults (enableDnsSupport defaults true; enableDnsHostnames defaults false except in a default VPC) — worth knowing because the default-false one is the one that bites. I tried to refute on the grounds that only enableDnsSupport is strictly needed for resolution; AWS's text does not support that narrowing, it names both. Consistency check passed: 10-route53.md already asserts this in prose at lines 111 and 197, and 05-vpc-core.md line 133 says the same thing in Terraform form (`enable_dns_hostnames` + `enable_dns_support`), so nothing in the vault contradicts it — the row only makes an already-stated fact drillable, which is exactly what §13.7 asks for. Row shape matches the Public|Private columns of the target table. Note for the human when applying: the Terraform argument names are snake_case on `aws_vpc`; the camelCase strings are the EC2 API/console-level names.

</details>

---

### [? · ? · 0 qid(s)] 01-iam-advanced.md — AWS Organizations has no authentication/federation feature — connecting a corporate directory is IAM Identity Center's job; existing accounts join by INVITATION (owner must accept).

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/organizations/latest/userguide/orgs_introduction.md

**Proposed:**
```markdown
### Organizations vs IAM Identity Center

|   | **AWS Organizations** | **IAM Identity Center** |
|---|---|---|
| What it owns | accounts, OUs, **consolidated billing**, SCP/RCP guardrails | workforce **sign-in**, the identity source, **permission sets** |
| Connect a corporate AD or external IdP | **not its job** — Organizations has no authentication or federation feature; its own docs hand the directory work to Identity Center | that *is* its job — identity source is an external **SAML 2.0 IdP**, **AD Connector** / **AWS Managed Microsoft AD**, or the built-in Identity Center directory |
| Bring an **existing** account under it | **invite** it, from the management account only; the owner must **accept** within **15 days** or it expires | n/a — it assigns access, it doesn't own accounts |
| Exam trigger | "one bill", "guardrail across every account", "consolidate our existing accounts" | "users sign in with their **existing corporate credentials**", "one login, many accounts" |
```

<details><summary>adversary reasoning</summary>

I fetched the Organizations 'What is' page myself (the .md endpoint returns real prose; the .html returns a JS shell, so I used .md throughout). Its seven feature headings are Manage your AWS accounts / Define and manage your organization / Secure and monitor your accounts / Control access and permissions / Share resources across accounts / Audit your environment for compliance / Centrally manage billing and costs. There is no authentication, federation, sign-in or directory-connection feature, and the one heading that sounds like it opens: 'Set up AWS IAM Identity Center to provide access to AWS accounts and resources using your active directory, and customize permissions based on separate job roles.' I also fetched the invitations page independently: 'you can invite existing AWS accounts to join your organization'; 'AWS Organizations sends an invitation to the account owner, who can decide to accept or decline'; 'Each invitation must be responded to within 15 days, or it expires'; 'You can send an invitation to other accounts only from the management account'. And manage-your-identity-source.md + manage-your-identity-source-ad.md confirm the identity sources are an external IdP, AD (AD Connector or AWS Managed Microsoft AD), or the built-in Identity Center directory. So the concept survives.

WHERE I NARROWED IT — one cell overstates. The draft writes 'It can't — Organizations has no authentication feature at all', and _selftest.py lifts table cells verbatim into a drilled ANSWER, so an absolute negative gets memorised. The same Organizations page's use-case list says: 'Share your AWS Directory Service for Microsoft Active Directory as a central identity store for your applications' — Organizations does have a directory-adjacent story (sharing a Managed AD via RAM). I

</details>

---

### [? · ? · 0 qid(s)] 01-iam-advanced.md — The distractor "enable the organization's external authentication" names a feature AWS Organizations does not have; consolidating existing accounts means inviting them, not writing SCPs to "manage" them.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_scps.md

**Proposed:**
```markdown
> [!warning] Trap — "enable the organization's external authentication"
> **AWS Organizations does not authenticate anybody.** Nothing in its feature list — accounts and OUs, policy guardrails, consolidated billing, security/audit/resource-sharing integrations — is an authentication, federation or directory-connection feature. AWS's own Organizations page hands that job to a different service: *"Set up AWS IAM Identity Center to provide access to AWS accounts and resources using your active directory."* So an option phrased *"enable the organization's external authentication and point it at the company's directory"* names a **feature that does not exist** — eliminate it on sight and reach for **IAM Identity Center**, with **AD Connector** or **AWS Managed Microsoft AD** as the identity source. The other half of the same question: to bring **existing** accounts under one organization you **invite** them and the owner **accepts** — you only *create* brand-new accounts. And **SCPs are not how you "manage" accounts**; they only cap what principals inside them may do.
```

<details><summary>adversary reasoning</summary>

Every factual limb holds up against docs I fetched myself. Organizations feature list (orgs_introduction.md) contains no authentication feature and explicitly delegates the directory job to IAM Identity Center. Invitations page confirms invite-and-accept for existing accounts. And I pulled the SCP page independently, which states: 'SCPs do not grant permissions to the IAM users and IAM roles in your organization. No permissions are granted by an SCP. An SCP defines a permission guardrail, or sets limits, on the actions that the IAM users and IAM roles in your organization can perform.' So 'SCPs are not how you manage accounts' is right.

WHERE I NARROWED IT — the draft says Organizations 'creates and groups accounts, aggregates billing, and applies policy guardrails — that is the entire feature list.' That sentence is false as written, and it becomes a drilled answer verbatim (_selftest.py lifts the whole trap body). The actual feature list also includes 'Secure and monitor your accounts' (GuardDuty, IAM Access Analyzer, Macie), 'Share resources across accounts' (AWS RAM, License Manager, Service Catalog) and 'Audit your environment for compliance' (CloudTrail, Config, Backup). Claiming a three-item exhaustive feature list would itself be a memorised error. I replaced it with an accurate statement that still carries the negative claim. I also made the Identity Center recommendation name both AD options rather than AD Connector alone, and tightened 'you don't create them' to the accurate 'you only create brand-new accounts'.

REDUNDANCY NOTE, not a refutation: this trap and draft 1's table row 2 assert the same underlying fact on two surfaces, which brushes against §13.3's "don't hand-write a fact twice". I still recommend both, because they drill different skills — the 

</details>

---

### [? · ? · 0 qid(s)] 01-iam-advanced.md — Central IT's administrative reach into member accounts comes from a cross-account IAM role (OrganizationAccountAccessRole, auto-created only for accounts the org creates) or Identity Center permission sets — membership itself gives billing and guardrails, not access.

**Verdict:** NARROWED  ·  **Citation:** https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_accounts_invites.md

**Proposed:**
```markdown
### Multi-account governance — what membership does and doesn't buy

*Separate accounts keep the divisions autonomous; that autonomy is the point. It leaves central IT two further problems with two different answers, and the exam asks you to pick both at once.*

| What central IT needs | What actually provides it | What does **not** |
|---|---|---|
| Cost oversight across every division | **Consolidated billing** — once an account joins, the management account becomes responsible for all charges the member accrues | separate per-account invoices — the member's own payment method is no longer used |
| **Administrative reach into a member account** | **A cross-account IAM role the management account assumes** — `OrganizationAccountAccessRole` is created automatically in accounts the org **creates**, and must be **added manually in invited accounts**. Or IAM Identity Center **permission sets**. | **being in the organization.** Membership buys billing and guardrails, **not access**. Trusted Advisor and Tag Editor only *inspect and report* — they grant nothing |
```

<details><summary>adversary reasoning</summary>

The load-bearing facts verify. I fetched orgs_manage_accounts_access-cross-account-role.md myself: 'When you create a member account using the AWS Organizations console, AWS Organizations automatically creates an IAM role named OrganizationAccountAccessRole in the account. This role has full administrative permissions in the member account... To use this role to access the member account, you must sign in as a user from the management account that has permissions to assume the role.' And the invitations page I fetched independently: 'For invited member accounts, AWS Organizations doesn't automatically create the IAM role OrganizationAccountAccessRole... you can manually add the role', plus the note 'When you create an account in your organization instead of inviting an existing account to join, AWS Organizations automatically creates the IAM role OrganizationAccountAccessRole by default' — which correctly generalises the draft's claim beyond the console. Billing: 'After an account accepts the invitation... the management account of the organization becomes responsible for all charges accrued by the new member account. The payment method attached to the member account is no longer used.' Permission sets: what-is.md confirms they are the Identity Center mechanism for AWS-account access, organization instances only. Trusted Advisor 'inspects your AWS environment, and then makes recommendations' (awssupport/latest/user/trusted-advisor.md) and Tag Editor 'enables you to effectively manage tags' (tag-editor/latest/userguide/tagging.md) — both advisory/metadata, neither grants access. So 'membership buys billing and guardrails, not access' stands.

WHERE I NARROWED IT — two problems, both in drilled answer cells.

(1) The cost row's 'What does not' cell reads 'per-account invo

</details>

---

## REFUTED — do not apply

- **13-cost-optimization.md** — How MUCH to commit: the commitment quantity is the level sustained most of the time, not the floor and not the peak; must-have 24x7 capacity never goes on Spot
  - Three independent failures, any one of which is disqualifying.

(1) THE NOVEL CLAIM IS UNSOURCED. Everything the draft adds beyond the note is the prescription 'commit at the level the usage actually sustains in most hours, not the minimum the fleet never drops below.' None of the six citations says this — they establish the hourly mechanics, not the sizing rule, and the drafter then reasons to th

- **13-cost-optimization.md** — Define 'baseline' in the existing worked example so it cannot be read as the conservative floor
  - The premise is false for the scenario it edits. The drafter says 'baseline' is 'undefined against a min/typical/peak scenario' — but the worked example at line 221 has no min/typical/peak. It opens 'A company runs roughly $12/hour of EC2 around the clock, but the mix changes' and the entire example turns on the *mix* changing (families, Fargate), not the *volume*. Usage is explicitly steady. So th

- **10-route53.md** — A static error/maintenance page when the origin is down is a CloudFront custom error response, not a Route 53 active-passive failover to an S3 site.
  - The trap's central assertion — that 'show a static maintenance/error page when the site is unavailable' is NOT a Route 53 answer — is contradicted by AWS's own Route 53 documentation, which describes that exact configuration as a customer pattern: 'Some customers use a web server as their primary resource and an Amazon S3 bucket that is configured as a website endpoint as their secondary resource.

- **10-route53.md** — De-emphasise the bolded "AWS resources only" claim in the drilled Alias vs CNAME table.
  - The drafter marked this `contradicts_note: true`. It does not. The existing cell already reads '**AWS resources only** (+ records in the same zone)' — the exception is stated in the same cell, so there is no wrong fact to correct and nothing for the human to un-memorise. This is a pure emphasis reword of a cell that _selftest.py blanks and drills, and churning a drilled cell for style has a cost w

- **10-route53.md** — Same de-emphasis in the three-property alias table under "Why a CNAME cannot sit at the apex".
  - Again flagged `contradicts_note: true` and again it does not: the existing cell at line 57 already says the target list includes 'or another record in the same zone'. The draft's stated rationale — that this row 'is what pushes the candidate toward CNAME' — is not supportable when the corrective fact sits inside the same cell, and the section's whole argument (lines 43-61) is that a CNAME at the a
